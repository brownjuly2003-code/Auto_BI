# Phase 3.2 — DataLens adapter: reversed payloads (auth + connection + dataset LIVE-verified)

Дата: 2026-06-13 (по «go» на 3.2). Продолжение спайка `2026-06-13-phase3.1-datalens-spike.md`.
Инстанс: self-hosted OSS DataLens на Mac (gateway v4.10.4, `HC=1`). Метод — прямые вызовы
backend'ов из docker-сети (`docker exec datalens-us node -e …`) + чтение исходников/логов
контейнеров. Всё ниже помеченное ✅ — **подтверждено живым ответом**, не угадано.

> Статус: **транспорт + auth + connection + dataset реверснуты и работают живьём.** Остаток
> (login-эндпоинт снаружи, блобы chart/dashboard, код адаптера+контракт-тесты) — в §«Открыто».
> Прерван на флапающем SSH-туннеле к Mac (см. §«Грабли»).

---

## 1. Транспорт (подтверждает вывод спайка 3.1)

Адаптер ходит через **UI-gateway** `POST http://<datalens-ui>:8080/gateway/root/<svc>/<method>`
(JSON, **cookie-сессия `auth`**). Это deployment-реалистично: в self-hosted деплое UI — единственная
публичная точка, бэкенды (control-api/us/auth/data-api) внутренние. Gateway сам расшифровывает
cookie и подмешивает `Authorization: Bearer <JWS>` бэкендам.

**Важно:** gateway корректно форвардит тело для `bi/createConnection` и `bi/createDataset`
(✅ оба отработали). Баг тела изолирован в `bi/validateDataset` (роняет тело → 415, см. 3.1) —
**мы его НЕ используем**: схему датасета даём детерминированно из IR (см. §4), валидация не нужна.

## 2. Auth ✅ (signin → cookie `auth`)

Auth-сервис (`@gravity-ui/expresskit`, Node) маршруты (из `dist/server/routes.js`):
`POST /signin` (passport local), `POST /refresh`, `GET /logout`, `POST /signup`,
`POST /private/v1/management/users/*` (master-token).

**Login ✅:** `POST {auth}/signin` JSON `{login:"admin", password:"admin"}` → `200 {"done":true}`,
ставит cookie **`auth`** (+ `auth_exp`). `auth` = **JWE (5 частей, encrypted)** — НЕ plain JWS;
control-api напрямую её не принимает («Invalid token»). Gateway расшифровывает её сам.

**Следствие для адаптера:** держать cookie-jar; `auth` cookie приложить ко всем `/gateway/root/*`
вызовам. (Для прямого control-api нужен был бы JWS, который минтит gateway — не пошли этим путём.)

control-api env (для справки): `AUTH__TYPE=NATIVE`, JWT `PS256` (`AUTH__JWT_KEY` = public). US:
`AUTH_ENABLED=true`, `MASTER_TOKEN=us-master-token` (US-REST CRUD по master-token ✅ — `DELETE
/private/entries/{id}` с `x-us-master-token` → 200; пригодится для chart/dashboard US-entry).

## 3. Connection ✅ (`bi/createConnection`)

`POST /gateway/root/bi/createConnection` (cookie) →
```json
{ "name": "...", "workbook_id": "<wb>", "type": "clickhouse",   // GP: "greenplum"
  "host": "host.docker.internal", "port": 8123, "username": "auto_bi_ro",
  "password": "...", "secure": "off",        // secure:"on"=HTTPS; CH :8123 plain → "off"
  "raw_sql_level": "subselect",              // ОБЯЗАТЕЛЬНО для dataset-from-SQL
  "cache_ttl_sec": null }
```
→ `200 {"id":"<connectionId-str>"}`. **Поле типа на входе = `type`** (НЕ `db_type`; `db_type`
только в ОТВЕТЕ `getConnection`). CH-host из BI = `host.docker.internal` (DataLens и
`auto_bi_clickhouse` в разных docker-сетях). `bi/createConnection` бэкенд = control-api
`POST /api/v1/connections`.

## 4. Dataset ✅ (`bi/createDataset`, схема из IR — без validate!)

`POST /gateway/root/bi/createDataset` (cookie) →
```json
{ "workbookId": "<wb>", "name": "auto_bi__<...>",
  "dataset": {
    "sources": [{
      "id": "<uuid>", "title": "<src>", "connection_id": "<connId>",
      "source_type": "PG_SUBSELECT",                 // CH: "CH_SUBSELECT"
      "parameters": { "subsql": "<generate_chart_sql(query)>" },
      "managed_by": "user",
      "raw_schema": [                                 // ← КЛЮЧ: колонки subselect из IR
        { "name": "category", "title": "category", "user_type": "string",
          "native_type": null, "nullable": true },
        { "name": "amount", "title": "amount", "user_type": "integer",
          "native_type": null, "nullable": true }
      ],
      "index_info_set": []
    }],
    "source_avatars": [{ "id":"<uuid>", "source_id":"<source.id>", "title":"<src>",
                         "is_root": true, "managed_by": "user" }],
    "avatar_relations": [],
    "result_schema": [                                // поля = колонки (из IR)
      { "guid":"<uuid>", "title":"category", "source":"category", "data_type":"string",
        "cast":"string", "type":"DIMENSION", "aggregation":"none", "calc_mode":"direct",
        "avatar_id":"<avatar.id>", "hidden":false, "managed_by":"user",
        "description":"", "formula":"", "valid":true }, ...
    ],
    "rls": {}, "component_errors": { "items": [] }
  } }
```
→ **`200`** ✅ (вернул dataset c `options.supported_functions` и т.д.; есть `id`).

**Почему без validate:** control-api `DatasetCreate` при создании нового источника читает
`source["raw_schema"]` (+ `index_info_set`) — если их нет, `KeyError: 'raw_schema'` → 500. Обычно
их выводит `validateDataset` (интроспектит БД). **Но наш адаптер знает колонки и роли из IR**
(`ChartQuery`: dimensions/measures/series/...), поэтому строит `raw_schema`+`result_schema`
детерминированно — это даже точнее инварианту #1 (детерминированный код адаптера, БД-introspection
не нужна). Маппинг IR-тип → `user_type` (DataLens `UserDataType` enum): string/integer/float/date/
datetime/boolean. `native_type:null` принят. Поля источника обязательны: `raw_schema`,
`index_info_set` (=[]), `parameters`, `managed_by`, `source_type`; у avatar — `managed_by`,
`is_root`. `raw_schema` item-схема = `RawSchemaColumnSchema` (`name,title,user_type,native_type?,
nullable,description?,has_auto_aggregation?,lock_aggregation?`).

**Идемпотентность:** как в Superset (имя `auto_bi__<slug>__<hash>`), для update —
`bi/updateDataset` (метод не реверсил; на 3.2 проверить).

## 5. Реверс остатка (2026-06-14, live на стенде через demo US-entries) + остаток кода

> Стек DataLens на Mac Up (13ч); реверс §5.1–5.3 сделан прямыми SQL к US-Postgres
> (`docker exec datalens-postgres psql -U pg-user -d pg-us-db`) — demo-workbook СОДЕРЖИТ
> 84 готовых чарта + 1 dash (в отличие от прежней заметки «чартов нет»), снято с них.

### 5.1 ✅ Login-эндпоинт ЗАПИНЕН (live 200 + cookie)

Путь = **`POST /gateway/auth/auth/signin`** (scope буквально `auth`, НЕ `root`). Тело
`{login,password}` → `200 {"done":true}` + `Set-Cookie: auth=…` (URL-enc JSON
`{accessToken,refreshToken}` с JWS PS256; gateway сам её принимает на `/gateway/root/*`).
Причина прежнего 401 на `/gateway/root/auth/signin`: scope `root` — auth-гейтед (циркулярно);
auth обслуживает выделенный роут `POST /gateway/:scope(auth)/:service/:action` c
`AuthPolicy.disabled` (`dist/server/components/auth/routes.js`). **Зашито в `client.py`
`DEFAULT_SIGNIN_PATH` (закрыт §5.1).** Транспорт RPC = `/gateway/root/<svc>/<method>` (cookie).

### 5.2 ✅ Chart (US widget-entry) `data`-блоб — РЕВЕРСНУТ

Структура entry.data = `{js, ui, url, graph, table, params, shared}`:
- **6 стабов** (js/ui/url/graph/table/params) — boilerplate, зовут `require('libs/datalens/v3')`
  с `ChartEditor.getSharedData()`/`getParams()`. Под нашим контролем → **генерить НАЧИСТО**
  (demo-стабы хранят искажённый плейсхолдер `$99` вместо тернарного `?` в `graph`-стабе — НЕ копировать).
  Стаб-провайдер по node-type: graph→`buildHighchartsConfig`(в `graph`), table→`buildChartsConfig`(в `table`),
  metric→как graph. `js`=buildGraph, `url`=buildSources, `ui`=buildUI, `params`=buildParams.
- **`shared`** (JSON-строка) — реальный конфиг: `{datasetsIds:[dsId], datasetsPartialFields:[[{guid,title,calc_mode}…]],
  visualization:{id,type,placeholders:[…]}, colors:[], sort:[], labels:[], filters:[], extraSettings:{…},
  type:"datalens", version:"4", updates:[], geopointsConfig:{}, hierarchies:[], shapes:[], …}`.

**Маппинг IR `Viz` → `visualization.id` (node) + раскладка placeholders** (снято live):

| IR Viz | viz_id | node | placeholders (роль→секция) |
|---|---|---|---|
| `big_number` | `metric` | metric_wizard_node | `measures`=[measures[0]] |
| `line` | `line` | graph_wizard_node | `x`=[dim0], `y`=measures, (`y2`=[]); series→`shared.colors` |
| `bar` | `column` | graph_wizard_node | `x`=[dim0], `y`=measures; series→`colors` |
| `stacked_bar` | `column` | graph_wizard_node | как bar + стек (наличие `colors`/неск. мер; 100%=`column100p`) |
| `area` | `area` | graph_wizard_node | `x`=[dim0], `y`=measures; series→`colors` |
| `pie` | `pie` | graph_wizard_node | `dimensions`=[dim0], `measures`=[measures[0]]; `sort`+`labels`=мера |
| `table` | `flatTable` | table_wizard_node | `flat-table-columns`=group_columns()+measures |
| `pivot` | `pivotTable` | table_wizard_node | `rows`=rows, `pivot-table-columns`=columns, `measures`=measures |
| `heatmap` | **нет в Wizard** | — | **деградация → `pivotTable`** (rows=[dim0],columns=[dim1],measures=[m0]) + пометка build-log (инвариант §3.4); нативный декартов heatmap только Editor/HC-путь |

**field-объект item** (в placeholders.items[] и colors/sort/labels) — берётся из dataset
result_schema, форма: `{type:DIMENSION|MEASURE, calc_mode:"direct", data_type, initial_data_type,
cast, aggregation, source:<alias>, guid:<field guid>, title, avatar_id:<dataset avatar>,
datasetId, datasetName, id:"dimension-N"/"measure-N", managed_by:"user", valid:true, hidden:false,
formula:"", description:"", autoaggregated:false, virtual:false, lock_aggregation:false,
aggregation_locked:false, has_auto_aggregation:false, guid_formula:"", default_value:null,
value_constraint:null}`. Биндинг к датасету — по `guid`+`avatar_id` из createDataset-ответа.
**РИСК (S5):** `visualization` в demo несёт ещё `name/icon/allowFilters/placeholders[].settings/
allowedTypes` — минимальный принимаемый набор полей проверить live-рендером (как form_data в Superset).

### 5.3 ✅ Dashboard (US dash-entry) `data`-блоб — РЕВЕРСНУТ

`{salt:<str>, schemeVersion:7, counter:<int>, tabs:[…], settings:{…}, description}`. Tab =
`{id, title, items:[…], layout:[…], aliases:{default:[[…]]}, connections:[…]}`:
- **`items[]`**: виджеты/текст/селекторы — `{id, type:"widget"|"text"|"control", data:{…}, namespace:"default"}`
  (widget.data ссылается на chartId; text.data.text=markdown).
- **`layout[]`**: грид-плейсмент `{i:<itemId>, x, y, w, h}` (по аналогии с position_json Superset; w в колонках).
- **`connections[]`** + **`aliases`**: связи селектор→чарт (scope-to-applicable, prep §A.3) —
  `{from:<selectorId>, to:<itemId>, kind:"ignore"|…}`, alias = пара `[fieldGuid, paramName]`.
- `settings`: `{autoupdateInterval, dependentSelectors:true, …}`.
Создаётся US-entry `scope:"dash"`. Селекторы (`spec.filters`) переносят
scope-to-applicable из `participating_chart_ids` (native_filters.py:54).

**✅ ENDPOINT РЕВЕРСНУТ + LIVE (2026-06-14)** — дашборд НЕ через приватный `_createEntry`
(он 404 на публичном gateway), а через gateway-экшен **`mix/createDashboardV1`**:
`POST /gateway/root/mix/createDashboardV1` тело
`{entry:{data, meta:null, workbookId, name}, mode:"publish"|"save"}`
(`keyOrWorkbookIdNameSchema` — camelCase `workbookId`+`name` для workbook-entry, ЛИБО `key`).
Экшен (`shared/schema/mix/actions/dash/create-dashboard-v1.js`) сам: инжектит
`schemeVersion`(=`DASH_CURRENT_SCHEME_VERSION`=**8**), `Dash.gatherLinks(data)`,
`Dash.validateData(data)`, затем `us._createEntry(scope=Dash, type:'')` → ответ
`{entry:{entryId, …}}`. Дашборд отдаётся на `GET /:entryId`.

**`data` = zod `dataSchema` МИНУС `schemeVersion`** (`shared/zod-schemas/dash.js`): top-level
`{salt:str≥1, counter:int≥1, tabs[], settings}`. `tabSchema` **`.strict()`** — ровно
`{id,title,items,layout,connections,aliases}`; `aliases` тоже strict (`{default?:[[..]]}`).
`settings` (НЕ strict, но required): `autoupdateInterval(num≥30|null)`,
`maxConcurrentRequests(num≥1|null)`, `silentLoading(bool)`, `dependentSelectors(bool)`,
`hideTabs(bool)`, `expandTOC(bool)` (+ optional loadPriority/hideDashTitle/globalParams).
Widget-item (tabItemSchema base `{id,namespace:"default",type:"widget"}` + widgetSchema
`{hideTitle, tabs:[{id,title,description,chartId,isDefault,params,autoHeight?}]}`).
`layoutItemSchema` `{i,h,w,x,y,parent?}`. **`validateData` (server) требует:** все id
(tabId/itemId/widgetTabId) уникальны; `items.length==layout.length` И каждый item имеет
ровно один layout-`i` == его id; connections ссылаются на валидные widget-tab/control id.

**🔑 `chartId` = ЭНКОДНУТЫЙ публичный entryId** (13-симв hashids), НЕ raw-numeric из US-PG:
US `Links.produceDbLinks` (`server/db/models/links/index.js`) делает `utils.decodeId(toId)`
на каждом link-target и на raw-numeric кидает `400 INCORRECT_LINK_ERROR{details.invalidLinkIds}`.
`create_chart` возвращает энкоднутый entryId от charts-engine → линк валиден (проверено:
raw `2236742093370295322` → encoded `2ldgiyvdtdqcm` через `server/utils.encodeId`, с ним
`mix/createDashboardV1`→`200 {entryId}`). Реализация: `adapter.py:assemble_dashboard` +
`build_dashboard_data`; LIVE E2E `adapter.build`→дашборд `3mfq6xulvrn2n` (bar+line, реальные CH).

### 5.4 ⏳ Остаток кода `auto_bi/adapters/datalens/` (следующая сессия, live-рендер)
   - ✅ **`client.py` НАПИСАН** (2026-06-14): `DataLensClient` — signin+cookie-jar `auth`+gateway
     POST `/gateway/root/<svc>/<method>`, зеркало `SupersetClient`. `DEFAULT_SIGNIN_PATH =
     /gateway/auth/auth/signin` — **live-запинен (§5.1)**.
   - ✅ **`dataset.py` НАПИСАН** (2026-06-14): `build_connection_payload`/`build_dataset_payload` —
     IR→§3/§4 payload детерминированно. Тип-маппинг `_user_type` (CH+PG спеллинги), `_measure_user_type`
     по agg, joined-колонки резолвятся из смежной таблицы, `source_type` CH/PG по engine, схема из IR
     (без validateDataset — инвариант #1), агрегация мер uniform `"sum"`=identity (зеркало
     form_data._adhoc_metric), стабильные uuid5. **14 unit-тестов на shape** (`tests/test_datalens_dataset.py`),
     стенд не нужен — pytest 253, ruff/black clean.
   - ✅ **`chart_config.py` НАПИСАН + LIVE-ВЕРИФИЦИРОВАН** (2026-06-14): `build_chart_shared` строит
     ТОЛЬКО `shared` (стабы добавляет charts-engine). Чарт = `POST /api/charts/v1/charts
     {data:shared, template:"datalens", workbookId, name}` (НЕ US `_createEntry` — тот приватный,
     через публичный gateway 404). viz_id-маппинг + placeholders как §5.2. Live: line+bar отрисованы
     с реальными данными CH (`/api/run`→highchartsConfig).
   - ✅ **`adapter.py` НАПИСАН** (2026-06-14): `DataLensAdapter`, 5 методов Protocol. ensure_database/
     ensure_dataset/create_chart **e2e live-верифицированы реальным кодом** (build→render). assemble_dashboard:
     блоб строится (`build_dashboard_data`), но **US dash-create endpoint НЕ реверснут → NotImplementedError**.
   - ✅ **`client.py`** +`post()` (charts-engine/`/api/run`).
   - **🔑 LIVE-НАХОДКИ (фиксы, реверс §3/§4 был неполон):**
     - **createDataset workbook = `workbook_id` (snake) в BODY, НЕ `workbookId`** (camel молча игнор → датасет
       orphaned, workbook_id NULL → **data-api 403 ACCESS_DENIED** при рендере; getPreview при этом работает).
       NB control-api тоже принимает workbook как header `x-dl-workbookid`, но для createDataset через gateway —
       snake в body. createConnection — `workbook_id` в body (как было).
     - **datetime → `genericdatetime`** (`DATASET_FIELD_TYPES`, НЕ «datetime»); иначе Type=`value_undefined`.
     - **createDataset СОХРАНЯЕТ переданные guid/avatar_id точь-в-точь** (`managed_by:"user"`) → чарт биндит по
       детерминир. guid из payload, re-fetch не нужен. raw_schema из `sources[].raw_schema` (`user_type`) корректно
       транспонируется в `source_collections[].origin.raw_schema` (`type`+`native_type:null`); `getDatasetByVersion`
       через gateway = 404 (не нужен).
     - login снаружи = `/gateway/auth/auth/signin` (§5.1).
   - ✅ **`adapter.py:assemble_dashboard` НАПИСАН + LIVE E2E (2026-06-14)**: `mix/createDashboardV1`
     (§5.3), `build` возвращает `DashboardRef`. Дашборд `3mfq6xulvrn2n` отрисован (bar+line, реальные CH).
   - ✅ **CONNECTION-ИДЕМПОТЕНТНОСТЬ + LIVE (2026-06-14 cont.)**: `ensure_database` ищет коннекшн
     по имени через **`us/getWorkbookEntries`** (`{workbookId, scope:"connection", filters:{name}}`,
     GET-экшен через gateway POST; имя — хвост `key`=`<numeric-id>/<name>` casefold, US лоуэркейсит
     ключи) → переиспользует encoded `entryId`, иначе создаёт (зеркало `SupersetAdapter`). Закрывает
     `400 «entity already exists»` на повторном build (CONNECTION_NAME константна). Live: переиспользован
     `ud58aoh1sfake`, encoded entryId валиден как `connection_id` для createDataset. `column`=авто-стек
     vert-bar (Wizard без grouped) ⇒ `STACKED_BAR→column` уже верен.
   - ✅ **СЕЛЕКТОРЫ (`spec.filters`) + LIVE-ФИЛЬТРАЦИЯ (2026-06-14 cont.2)**: `build_selectors`
     (adapter.py) → dash control-items, scope-to-applicable по grain (зеркало `superset.native_filters`).
     TIME→`elementType:"date"`+`isRange`, иначе `select` multiselect. **Кросс-датасет линковка
     через `aliases.default=[[guid,…]]`** — единственный `DashTabConnectionKind`=`"ignore"` (связи
     позитивны по умолчанию: один датасет ИЛИ alias-группа), поэтому out-of-scope чарт не затрагивается
     без негативной разводки; per-dataset field-guid'ы in-scope датасетов кладутся в одну alias-группу.
     Контрол: `data` несёт `id/namespace/title/sourceType/source`, `source.defaultValue` ОБЯЗАТЕЛЕН
     (zod; демо-блоб без него = старая схема), `defaults:{guid:""}`. In-scope чарты теряют LIMIT
     (`apply_limit=False`, `participating_chart_ids`). Live (dash `o70ox316qay08`): выбор store=1 →
     table→1 строка, KPI=grand total (excluded), 2-й in-scope датасет отфильтрован через alias.
   - **Остаток 3.2:** идемпотентность dataset/chart/dashboard (повтор ОДНОГО title коллизит по
     уникальному ключу — deferred in-place-update, как в Superset; нужен `bi/updateDataset`/chart-update).

## 6. Грабли стенда

- **SSH-туннель Mac:8080→Win:8090 флапает** («closed by remote host», exit 255 каждые ~1-2 мин —
  память/sshd Mac). Реверс делать прямыми `docker exec … node -e` внутри сети (надёжнее туннеля);
  туннель только для браузера. Перезапуск: `ssh -N -o ServerAliveInterval=15 -L 8090:localhost:8080 deproject-mac`.
- В `datalens-us`/`datalens-auth` нет wget/curl → HTTP через `node -e` (http-модуль).
- `docker` не на PATH в non-login SSH → `/usr/local/bin/docker`.
- Тест-артефакт: 1 датасет `spike_ds_<ts>` в demo-workbook `z4wtz6tg5194o` (безвреден; удалить через
  US `DELETE /private/entries/{id}` master-token при случае). Тест-коннекшны 3.1 уже удалены.
- DataLens-стек на Mac Up; `auto_bi_greenplum` остановлен (вернуть `docker start`+туннель 15433).
</content>
