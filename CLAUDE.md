# CLAUDE.md — Auto_BI

Агент «текст/раскладка полей → дашборд в выбранной BI» поверх DM-слоя DWH. Полный контекст: `README.md`, `docs/ARCHITECTURE.md` (дизайн), `docs/PLAN.md` (фазы), `docs/MARKET.md` (рынок/зачем).

## Статус

**ТЕКУЩЕЕ (2026-06-14): PHASE 3.2 DATALENS-АДАПТЕР — DASH-CREATE ENDPOINT РЕВЕРСНУТ, `assemble_dashboard` РЕАЛИЗОВАН, ПОЛНЫЙ E2E LIVE-ВЕРИФИЦИРОВАН (по «продолжи»), ветка `phase-3/datalens-dashboard-endpoint`.** Закрыт главный остаток 3.2 — теперь адаптер собирает дашборд целиком. (1) **Эндпоинт**: дашборд = US-entry scope=`dash` через gateway-экшен **`mix/createDashboardV1`** (`POST /gateway/root/mix/createDashboardV1`), НЕ приватный `_createEntry` (тот 404). Args `{entry:{data, meta:null, workbookId, name}, mode:"publish"}` (`keyOrWorkbookIdNameSchema` camelCase: `workbookId`+`name` для workbook-entry); экшен сам инжектит `schemeVersion`(=8), собирает links (`Dash.gatherLinks`) и гоняет `Dash.validateData` server-side, затем `us._createEntry(scope=Dash, type:'')`. (2) **`data`-блоб** (`build_dashboard_data`) пересобран строго под zod `dataSchema` МИНУС `schemeVersion`: top-level `salt`+`counter`(int≥1); `tabs[]` **`.strict()`** — ровно `id/title/items/layout/connections/aliases`; `settings` со ВСЕМИ required (`autoupdateInterval/maxConcurrentRequests/silentLoading/dependentSelectors/hideTabs/expandTOC`); widget-item `{id,namespace:"default",type:"widget",data:{hideTitle,tabs:[{id,title,description,chartId,isDefault,params}]}}`; layout `{i,x,y,w,h}` (2 колонки ×12). `validateData` требует уникальные id + `layout.length==items.length` с совпадающими `i` — соблюдено (item-id / widget-tab-id / tab-id различны). (3) **🔑 LIVE-НАХОДКА**: `chartId` в widget-tab ДОЛЖЕН быть ЭНКОДНУТЫЙ публичный entryId (13-симв hashids, как `z4wtz6tg5194o`), НЕ raw-numeric из US-Postgres — US `Links.produceDbLinks` делает `decodeId(chartId)` и на raw-numeric кидает `400 INCORRECT_LINK_ERROR{invalidLinkIds}`. `create_chart` возвращает именно энкоднутый entryId от charts-engine → линк валиден (raw-numeric `2236742093370295322`→encoded `2ldgiyvdtdqcm` через US `server/utils.encodeId`). (4) **`adapter.py`**: `assemble_dashboard` зовёт gateway-экшен и возвращает `DashboardRef(id,title,url="/{entryId}")` (DataLens отдаёт entry на `GET /:entryId`); `build` теперь возвращает `DashboardRef` (зеркало `SupersetAdapter.build`), а не список чартов. **E2E LIVE через туннель :8090** (реальный `DataLensAdapter.build` на стенде Mac): connection→2 датасета→2 чарта→дашборд `3mfq6xulvrn2n`, открыт в браузере — bar «выручка по магазинам» + line «выручка по дням» отрисованы с РЕАЛЬНЫМИ данными CH (~80M / ~480M), консоль чистая, скрин `D:\.playwright-mcp\datalens_dashboard_e2e_rendered.png`. **pytest 264** (3 dash-теста обновлены: build-цепочка вкл. createDashboardV1, блоб-shape под zod, assemble_dashboard линкует encoded chartId), ruff/black clean.

**Затем (2026-06-14 cont., по «продолжи»): CONNECTION-ИДЕМПОТЕНТНОСТЬ ДОБАВЛЕНА + LIVE.** `CONNECTION_NAME` константна → на КАЖДОМ повторном build было `400 «entity already exists»`. `ensure_database` теперь сперва ищет коннекшн по имени через **`us/getWorkbookEntries`** (`{workbookId, scope:"connection", filters:{name}}`; имя матчится по хвосту `key`=`<numeric-id>/<name>` casefold — US лоуэркейсит ключи) → переиспользует encoded `entryId`, иначе создаёт (зеркало `SupersetAdapter`, idempotent-by-name). **LIVE**: `ensure_database` переиспользовал существующий `ud58aoh1sfake` (дубль не создан), полный `build` с дефолтным именем прошёл (датасет забиндился к переиспользованному коннекшну — encoded entryId валиден как `connection_id`), дашборд `h0tbvm4t1z2i1`. **pytest 265** (+`test_ensure_database_reuses_existing_connection`), ruff/black clean. Из схемы: `column` DataLens — авто-стек vert-bar (grouped-варианта в Wizard нет) → `STACKED_BAR→column` уже семантически верен, отдельной правки стека не требует.

**Затем (2026-06-14 cont.2, по «go»): СЕЛЕКТОРЫ (`spec.filters`) РЕАЛИЗОВАНЫ + LIVE-ВЕРИФИЦИРОВАНЫ ФИЛЬТРАЦИЕЙ.** `build_selectors` компилирует `spec.filters` в dash control-items (зеркало `superset.native_filters`): **scope-to-applicable** по grain (фильтр действует на чарт только если колонка в его `group_columns()`); TIME-колонка → `elementType:"date"` + `isRange`, иначе `select` multiselect. **🔑 Кросс-датасет линковка через `aliases`**: в DataLens единственный `DashTabConnectionKind`=`"ignore"` (связи позитивны по умолчанию — один датасет ИЛИ поля, связанные `aliases.default=[[guid,…]]`), поэтому out-of-scope чарт (его датасет не содержит поля) НЕ затрагивается без негативной разводки; in-scope датасеты связываются по per-dataset field-guid одной alias-группой. Контрол биндится к первому in-scope (`source.datasetFieldId`=guid, `defaults:{guid:""}`); `data` контрола несёт `id/namespace/title/sourceType/source` + `source.defaultValue` (демо-блоб без них = старая схема, но `mix/createDashboardV1` zod-валидирует строго). In-scope чарты теряют top-N LIMIT (`apply_limit=False` через `build`→`ensure_dataset`→`build_dataset_payload`→`generate_chart_sql`; `participating_chart_ids` из spec) — иначе опции селектора капались бы пре-LIMIT топ-N. **LIVE E2E** (дашборд `o70ox316qay08`, bar+table по store_id + KPI total): селектор «ID магазина» открылся со ВСЕМ списком store_id (LIMIT снят), выбор store=1 → **table сузилась до 1 строки 54.8M, KPI остался 236.1B (grand total — scope-exclusion работает), table=2-й in-scope датасет ⇒ alias-пропагация подтверждена**, консоль чистая, скрины `D:\.playwright-mcp\datalens_selector_{e2e,filtered}.png`. **pytest 270** (+5 selector-тестов: scope/alias/time/skip/layout), ruff/black clean. Грабли: SSH-туннель Mac:8080→Win:8090 флапает (exit 255 ~1-2мин) — реверс эндпоинтов делать `docker exec -i <svc> node` (надёжно), туннель только для финального Python-E2E/браузера бёрстом.

**Затем (2026-06-14 cont.3, по «продолжи»): ИДЕМПОТЕНТНОСТЬ dataset/chart/dashboard РЕАЛИЗОВАНА + LIVE-ВЕРИФИЦИРОВАНА — последний остаток 3.2 закрыт, ветка `phase-3/datalens-idempotency`.** US enforces unique entry keys per workbook+scope → повтор ОДНОГО title коллизил `400 entity already exists` у dataset, chart И dashboard (не только connection; в отличие от Superset, где имена чартов/дашбордов дублируются и переиспользуется лишь dataset). Решение — **delete-then-create единообразно** через новый `_delete_if_exists(scope, name)`: `_find_entry_id` (точный match по `key`-хвосту; server `filters.name` работает и для кириллицы — live-проверено) → если есть, удалить → обычный create. **🔑 транспорт-находка:** приватный `us/_deleteUSEntry` через cookie root-gateway = `404 UNKNOWN_SERVICE_ACTION` (underscore-экшены НЕ экспонированы); рабочий путь — **`mix/deleteEntry {entryId, scope}`** (не underscore), роутит по scope: `dataset`→`bi.deleteDataset`, `connection`→`bi.deleteConnection`, иначе (`widget`/`dash`)→внутренний `us._deleteUSEntry` (server-side, минует публичный gateway; widget уезжает в `__trash/`, ключ освобождается). update-in-place (`bi/updateDataset`/chart/dash) сознательно НЕ делал: delete+create даёт ту же видимость (id датасета/чарта внутренние; URL дашборда и так меняется per build, как в Superset) — проще и без реверса 3 update-эндпоинтов. Connection остаётся reuse-by-name (его encoded id референсят датасеты, не удаляем). **LIVE E2E**: один и тот же spec (`Auto_BI idempotency CONST`, кириллица + селектор) построен ДВАЖДЫ свежими адаптерами — 1-й создал, 2-й **заменил** все 5 коллизий (2 dataset + 2 widget + dash) и прошёл без `entity already exists`; пересозданный дашборд `n61l2ey7497u7` открыт в браузере — bar (~80M/store) + KPI с реальными данными CH, **0 ошибок в консоли** (binding chart→dataset пережил delete+recreate), скрин `D:\.playwright-mcp\datalens_idempotency_rebuilt_rendered.png`. **pytest 271** (+`test_build_replaces_existing_dataset_chart_dashboard`; build теперь делает доп. getWorkbookEntries-lookup перед каждым create), ruff/black clean. **🏁 ВСЕ ЗАДАЧИ 3.2 ЗАКРЫТЫ (адаптер: connection/dataset/chart/dashboard/selectors/идемпотентность, полный E2E).** Реверс delete: §5.5 reversal-дока.

**Затем (2026-06-14 cont.4, по «продолжи»): 3.5 DATALENS-СБОРКА EVAL ЗАКРЫТА → 🏁🏁 PHASE 3 ЗАВЕРШЕНА (S6), ветка `phase-3/datalens-contract`.** Добавлен live contract-сьют `tests/test_datalens_contract.py` (integration-gated, зеркало `test_superset_contract.py`) — детерминированная часть 3.5 «spec собирается в DataLens». **11/11 live** против self-hosted стенда: 10 viz-кейсов (big_number/line/bar/stacked_bar/area/pie/table/pivot/heatmap→pivotTable/**join**) — каждый IR→dataset→chart, `/api/run` отдаёт реальные данные CH (viz-агностичный `_rendered_with_data`: metric=list, graph=`data.graphs[].data`, table=`data.rows`); + full `build()` с селектором build×2 (идемпотентность). LLM-golden для DataLens НЕ нужны — IR BI-агностичен (инвариант 1), покрыт общим golden-сьютом; «DataLens-сборка» = детерминированный build-контракт. **🔑 НАХОДКА contract-теста → хардена адаптера:** charts-engine/US **валидируют charset имени entry** (`data.name should start/end with [A-Za-zА-Яа-яЁё0-9_@()%], contain only [...().,:;'|-–—−$*&]+spaces`) → title с `[]`/`/`/`?`/`#` роняли create (`400 VALIDATION_ERROR`; Superset такого не имеет). Добавлен `safe_entry_name()` (dataset.py) — санитизация имён chart/dashboard-entry (replace disallowed→space, collapse, trim до edge-allowed, fallback `Auto_BI`); применён в `create_chart`/`assemble_dashboard` + idempotency-lookup'ах (датасеты уже ASCII-safe). +настройки `AUTO_BI_DATALENS_*` в config (url/user/password/workbook_id=`z4wtz6tg5194o`/`ch_host_from_datalens`=host.docker.internal). **pytest 272** (+`safe_entry_name` unit; integration 11 gated/deselected по умолчанию → «272 passed, 32 deselected»), ruff/black clean. **Exit-критерии Phase 3 ВЫПОЛНЕНЫ:** один spec собирается и в Superset, и в DataLens (live contract обеих сторон) + GP advisor live на 10.3M. **S6: Phase 4 НЕ начинать — предложить ревью (`/cxkm`). Дальше — по «go».**

**Ранее (2026-06-14): PHASE 3.2 DATALENS-АДАПТЕР — `chart_config.py`+`adapter.py` НАПИСАНЫ, ПОЛНЫЙ E2E LIVE-ВЕРИФИЦИРОВАН (по «продолжай»), ветка `phase-3/datalens-adapter-impl`.** Реальный код адаптера на живом self-hosted стенде (Mac) построил connection→dataset→chart и **отрисовал line- и bar-чарты с РЕАЛЬНЫМИ данными CH** (`/api/run`→highchartsConfig+данные; скрины `D:\.playwright-mcp\datalens_*_rendered.png`). (1) **`chart_config.py`** `build_chart_shared` — IR viz→DataLens `shared` (visualization.id+placeholders+field-items); чарт = US widget-entry через `POST /api/charts/v1/charts {data:shared, template:"datalens"}` (charts-engine сам добавляет JS-стабы — стабы писать НЕ надо); маппинг big_number→`metric`/line→`line`/bar+stacked→`column`/area→`area`/pie→`pie`/table→`flatTable`/pivot→`pivotTable`/heatmap→pivotTable(деградация+лог). (2) **`adapter.py`** `DataLensAdapter` — 5 методов Protocol; ensure_database=createConnection, ensure_dataset=createDataset, create_chart=charts-engine, **assemble_dashboard: блоб строится (`build_dashboard_data`, §5.3) но US dash-create endpoint ещё НЕ реверснут → NotImplementedError** (остаток). (3) **`client.py`** +`post()` для charts-engine. **Live-находки (фиксы в коде, unit-тесты не поймали бы):** ⚠️ **createDataset workbook: `workbook_id` (snake) в body, НЕ `workbookId`** — иначе датасет orphaned (workbook NULL) → data-api 403 ACCESS_DENIED при рендере; ⚠️ **datetime→`genericdatetime`** (enum DataLens `DATASET_FIELD_TYPES`, не «datetime»); createDataset **сохраняет мои детерминир. guid/avatar точь-в-точь** (чарт биндит по ним без re-fetch); login-path `/gateway/auth/auth/signin`. **pytest 264 (+11 chart-тестов), ruff/black clean.** Остаток 3.2: dash US-create endpoint (реверс) + stacked_bar-стек/heatmap-семантика (рендерятся, точность позже). Дальше — по «go».

**Ранее (2026-06-14): PHASE 3.2 DATALENS-АДАПТЕР — КОД `client.py`+`dataset.py` НАПИСАН (стенд-free часть, по «продолжи»), ветка `phase-3/datalens-adapter-code`.** Реализован пакет `auto_bi/adapters/datalens/`: (1) **`client.py`** — `DataLensClient` (signin → cookie-jar `auth` → UI-gateway `POST /gateway/root/<svc>/<method>`, зеркало `SupersetClient`; **login-path LIVE-ЗАПИНЕН = `/gateway/auth/auth/signin`** — scope буквально `auth` через выделенный роут `:scope(auth)` c `AuthPolicy.disabled`, прежний `root/auth/signin`→401; закрыт §5.1). (2) **`dataset.py`** — `build_connection_payload`/`build_dataset_payload`: IR → `bi/createConnection`/`bi/createDataset` детерминированно (реверс §3/§4). Тип-маппинг native→DataLens `user_type` для CH+PG спеллингов (Nullable/LowCardinality/Decimal/timestamp/...); measure-тип по agg (count→integer, avg→float, sum/min/max→семья колонки); joined-колонки резолвятся из смежной таблицы по bare-алиасу; `source_type` CH_SUBSELECT/PG_SUBSELECT по engine; **схема `raw_schema`+`result_schema` строится из IR без `validateDataset`** (инвариант #1); агрегация мер = uniform `"sum"` = identity над пре-агрегированным subselect (зеркало `form_data._adhoc_metric`); стабильные uuid5 (идемпотентность). **14 unit-тестов на shape + client-транспорт** (httpx.MockTransport). pytest **253**, ruff/black clean. **2026-06-14 live-реверс остатка (стек DataLens на Mac Up, через demo US-entries в `pg-us-db`):** (a) login-path запинен (выше). (b) **chart-блоб РЕВЕРСНУТ** (§5.2): entry.data=`{js,ui,url,graph,table,params,shared}`; стабы — boilerplate `libs/datalens/v3` генерить начисто (demo хранит искажённый `$99` вместо `?`); `shared` несёт visualization+placeholders; **маппинг IR Viz→`visualization.id`**: big_number→`metric`, line→`line`, bar/stacked_bar→`column`(+стек), area→`area`, pie→`pie`, table→`flatTable`, pivot→`pivotTable`, **heatmap→нет в Wizard → деградация в `pivotTable`+build-log** (нативный только Editor/HC); field-объект item-форма снята (guid/avatar_id/source/aggregation/...). (c) **dashboard-блоб РЕВЕРСНУТ** (§5.3): `{salt,schemeVersion:7,tabs:[{items,layout,aliases,connections}],settings}`. **Остаток 3.2 (живой стенд + live-рендер, S5):** написать `chart_config.py` (спека §5.2 готова — детерминированно+shape-тесты, семантику принять рендером) + `adapter.py` (5 методов Protocol) + контракт-тесты live e2e (login→createConnection→createDataset→createWidget→createDash→рендер). Дальше — по явному «go». Ранее реверс: документ `docs/plans/2026-06-13-phase3.2-datalens-adapter-reversal.md` (все payload'ы).

**Ранее (2026-06-13): PHASE 3.2 DATALENS-АДАПТЕР — РЕВЕРС auth+connection+dataset LIVE-ПОДТВЕРЖДЁН (по «go»), ветка `phase-3/datalens-adapter`.** Документ: `docs/plans/2026-06-13-phase3.2-datalens-adapter-reversal.md` (все payload'ы). Реверс прямыми `docker exec datalens-us node -e` внутри docker-сети (надёжнее флапающего туннеля). **Подтверждено живьём (✅):** (1) **auth**: `POST {auth}/signin {login:"admin",password:"admin"}` → `200`, ставит cookie **`auth`** (JWE, 5 частей — расшифровывает сам gateway; control-api напрямую её не берёт). Адаптер: cookie-jar + `/gateway/root/*`. (2) **createConnection** `bi/createConnection {name,workbook_id,type,host,port,username,password,secure,raw_sql_level:"subselect",cache_ttl_sec}` → `200{id}`. (3) **createDataset** `bi/createDataset` → **`200`** с источником `{source_type:"{PG|CH}_SUBSELECT",parameters.subsql,managed_by,**raw_schema**,index_info_set:[]}` + avatar `{is_root,managed_by}` + `result_schema` — **схему строим из IR детерминированно** (control-api при create читает `source.raw_schema`; обычно её выводит сломанный `validateDataset`, но мы знаем колонки из IR → validate НЕ нужен, инвариант #1). Gateway форвардит тело корректно для create*-методов; баг изолирован в `validateDataset`. **Открыто (нужен стабильный стенд):** login-эндпоинт СНАРУЖИ (через публичный gateway пути 404/401 — `/signin` живёт на внутреннем auth:8080; найти UI-прокси), блобы **chart (US widget-entry)** и **dashboard (US dash-entry)** — снять с созданных через Wizard (в demo-workbook чартов нет), затем код `adapters/datalens/` (client/dataset ГОТОВЫ к кодингу+unit; chart_config после блоба) + контракт-тесты live. Грабли: SSH-туннель Mac:8080→8090 флапает (~1-2мин); реверс — через docker exec; тест-датасет `spike_ds_*` в demo-workbook (безвреден).

**Ранее (2026-06-13): СПАЙК 3.1 DATALENS ВЫПОЛНЕН — вердикт GO (условный), ветка `phase-3/datalens-spike`.** Реверс API с живого self-hosted инстанса (Playwright-драйв UI + прямые gateway-`fetch` cookie-auth + логи backend-контейнеров на Mac). Отчёт: `docs/plans/2026-06-13-phase3.1-datalens-spike.md`. **Главное:** (1) **CH и GP коннекторы оба есть** нативно; **`HC=1` подтверждён** в env `datalens-ui` → Highcharts + нативный декартов heatmap (деградация prep §A.2 снимается). (2) **entryId — строки** (S4-2 валиден). (3) **createConnection live-подтверждён программно**: `bi/createConnection {name,workbook_id,type:"clickhouse",host,port,username,password,secure,raw_sql_level:"subselect",cache_ttl_sec}` → `200 {id}` (поле типа на входе = `type`, не `db_type`; CH-host из BI = `host.docker.internal`, не container-имя). (4) **КРИТИЧНО — gateway-баг (УТОЧНЕНО в 3.2 выше: финальный транспорт = ЧЕРЕЗ UI-gateway с cookie, gateway форвардит create* корректно; «control-api напрямую» был первичной гипотезой спайка).** Причина: gateway-экшен `validateDataset` (v4.10.4) роняет тело (форвардит на control-api `POST /api/v1/datasets/validators/dataset` c `Content-Type: x-www-form-urlencoded` + `Content-Length: 0` → 415, ломает создание датасета и в UI); прямой вызов control-api с `application/json` → 401(нужен auth), НЕ 415 ⇒ это баг gateway, не API. (5) **Auth-швы:** control-api `AUTH__TYPE=NATIVE` JWT PS256 — нужен user-JWT из `datalens-auth` (admin/admin); US `MASTER_TOKEN=us-master-token` — **US-REST CRUD по master-token live-подтверждён** (`DELETE /private/entries/{id}` `x-us-master-token` → 200). (6) **Чарты/дашборды = US-entry** (scope `widget`/`dash`) — на self-hosted создаются записью в US **любым блобом** (Wizard ИЛИ Highcharts-Editor; облачное «только createEditorChart» НЕ действует). (7) Dataset-from-SQL: subselect-источник `{PG|CH}_SUBSELECT parameters.subsql`, формат validate `updates:[add_source, add_source_avatar]` снят; модель `result_schema/source_avatars/avatar_relations`. **Остаток на 3.2 (механический реверс против live):** auth-клиент (JWT-login) + точные `data`-блобы chart/dashboard (в demo-workbook готовых чартов нет — снять с одного, созданного через Wizard) + end-to-end. **Грабли:** SSH-туннель к Mac флапает (~1-2 мин, работать burst'ами/прямыми вызовами); тест-коннекшны спайка удалены (уборка). Дальше — 3.2 по явному «go», либо GP-трек независимо.

**Ранее (2026-06-13): DATALENS SELF-HOSTED ДОСТУП СОЗДАН (по «делай сам доступ», ветка `phase-3/datalens`).** Yandex Cloud Public API недоступен (нет аккаунта; нужна телефон-верификация/биллинг — кредов нет и не будет). Поднял **open-source self-hosted DataLens** (`github.com/datalens-tech/datalens`) на Mac-стенде: `HC=1 docker compose up -d` (11 сервисов из ghcr.io), UI на Mac :8080, **логин admin/admin проверен через Playwright** (редирект `/collections`, приложение usable, скрин `D:\.playwright-mcp\datalens_authenticated_collections.png`), туннель Windows `ssh -N -L 8090:localhost:8080`. **HC=1 → Highcharts включён** (нужно спайку). Ключевые грабли (в runbook `2026-06-13-datalens-selfhosted-runbook.md`): (1) полный путь `/usr/local/bin/docker` в non-login SSH; (2) **DNS-фикс ghcr.io** — dockerd Go-резолвер не резолвит ghcr.io через gvproxy (glibc getent резолвит), добавил `nameserver 8.8.8.8/1.1.1.1` в resolv.conf VM ([[de-mac-docker-env]]); (3) temporal/meta-manager — гонка с postgres, перезапуск после healthy. `auto_bi_greenplum` остановлен ради RAM (вернуть: `docker start`). Тот же формат чартов, что у cloud-API → валидно для спайка/адаптера; отличается только auth-шов. **Спайк 3.1 (реверс createConnection/Dataset/EditorChart-HC/Dashboard payload с живого инстанса) — РАЗБЛОКИРОВАН, следующий шаг.** Адаптер 3.2 (`adapters/datalens/`) — после; шов ref `id:int|str` (S4-2) готов.

**Ранее (2026-06-13): GP at-scale advisor-правила LIVE-ВАЛИДИРОВАНЫ на ≥10M (по «продолжи»).** Ветка `phase-3/gp-scale-validation`. Закрыт остаток «скейл ≥10M» GP-трека: долил демо-факт `dm.sales` до **10.3M** строк на живом GP-стенде (`scripts/stand_scale_gp_dm.sql`, батчи 2M через stdin — память Mac реклеймится, OOM нет), интроспектор корректно просуммировал reltuples партиц-детей → `rows: 10300000`. Live-прогон advisor (`scripts/gp_scale_validate.py`, через туннель): **chart без фильтра → `distribution_skew` (WARN/DCR, store_id n_distinct=20 из pg_stats) + `no_filter_on_large_fact` (CRITICAL)**; chart с date-фильтром → только `distribution_skew` (фильтр снял no_filter). Оба правила прежде были только unit-тестированы. Влито в main `33a01fc`. **Затем (ветка `phase-3/gp-multilevel-partitions`): multi-level/list-партиции в интроспекторе** — `_partition_key` читает партиц-колонки со ВСЕХ уровней `pg_partition` (фильтр `paristemplate=false`, ORDER BY `parlevel`), не только `parlevel=0`; 2-уровневая RANGE(date)→LIST(region) → `partition_key='date, region'`, одноуровневая → `'date'` (обратно совместимо); структура каталога реверснута с живого GP (фикстура `dm.sales_ml`, дропнута после), live+unit `test_partition_key_multi_level_ordered_by_level`. Канонический демо остаётся 300k (скейл opt-in; `model_gp.yaml` коммитится из 300k, валидатор dump'ит в `.tmp/`). pytest **238**, ruff/black clean. Runbook обновлён (секции «live-валидация НА СКЕЙЛЕ» + «Multi-level/list-партиции»). **Затем (та же сессия): eval 3.5 — GP advisor-сьют + engine-dispatch.** `GP_ADVISOR_CASES` (`auto_bi/eval/cases.py`): детерминированные анти-паттерны на `dm.sales` (`no_filter_on_large_fact`+`distribution_skew` с seed `rows=20M`, `non_colocated_join` join мимо dist key, `group_by_high_cardinality`) + 2 clean; `_eval` (cli.py) берёт движок из `physical.engine` и выбирает набор кейсов (`advisor_cases_for_engine`/`golden_cases_for_engine`). `auto_bi eval --model-path semantic/model_gp.yaml --suite advisor` → 6/6 offline (без стенда/GraceKelly); CH-сьют 9/9 без регрессии. Unit `test_gp_advisor_suite_passes_on_committed_gp_model`. **Затем (по «делай что нужно, Fable отключили, используй агентов»): GP golden dialogue-кейсы — `GP_GOLDEN_CASES`, 14 шт** (8 clear + 1 iteration + 2 ambiguous + 3 infeasible) на GP-схеме `dm.sales`/`stores`/`products`. S2 (дизайн eval) делегирован **субагенту** (Sonnet, замена отключённого Fable); оркестратор независимо перепроверил live: **14/14 PASS через GraceKelly/Sonnet** (`--model-path semantic/model_gp.yaml --suite golden`), 2 GK-флейка (`unknown_error Browser execution` на g3/a1) прошли на ретрае. golden-сьют НЕ строит дашборды и НЕ зовёт EXPLAIN → нужен только GraceKelly + `model_gp.yaml` (GK живёт на **Windows** :8011, не Mac). pytest **239** (golden гейтятся live). **GP-часть eval 3.5 закрыта.** Остаток GP: сам Greengage (нужен образ; OOM-риск 2-го контейнера на 8GB Mac), REPLICATED dims / large-large motion на скейле (маргинально — правила уже live). **DataLens 3.1/3.2 — gate: нет IAM-токена/org-id/workbook (в `D:\TXT` и `.env` отсутствуют).** Дальше — только по явному «go».

**Ранее (2026-06-13): PHASE 3 ENGINE-ТРЕК (Greenplum/Greengage, 3.3+3.4) РЕАЛИЗОВАН И ВЛИТ В MAIN (`ce9ff2c`) — по «делай на маке то, что нужно».** Второй DWH живьём на одиночном Greenplum 6.25 (Mac, `andruche/greenplum:6`, контейнер `auto_bi_greenplum` порт 5433 — 5432 занят нативным PG). (1) **Dialect seam**: `auto_bi/engine.py` (engine→sqlglot dialect: clickhouse / greenplum→postgres), `generate_chart_sql(query, dialect=…)`, `guard_sql`/`LiveSQLValidator` per-engine trial-run (CH `SETTINGS` vs PG `SET statement_timeout`); CH-путь байт-в-байт прежний. (2) **Introspector** `introspect/greenplum.py`: PG-каталоги + `pg_get_table_distributedby`/`pg_partition`/`pg_stats.n_distinct`, reltuples по партиц-детям, FK-guess пропускает self-ref; `make_run_query_pg` (psycopg); live → `semantic/model_gp.yaml`. (3) **Advisor** `advisor/greenplum.py`: `non_colocated_join`/`partition_not_pruned`/`distribution_skew` + `gp_explain_evidence` (motion-узлы + «Partitions selected»); `advisor/core.py` выбирает пак/evidence/диалект по `physical.engine`. `Physical.distribution_key` (аддитивно). **Live-валидация**: postgres-SQL исполнился на GP; motion-join→`non_colocated_join` (Broadcast/Redistribute Motion), фильтр не по партиции→`partition_not_pruned` (7 of 7), co-located+date→clean. **pytest 237, ruff/black clean.** Runbook: `docs/plans/2026-06-13-phase3-greenplum-runbook.md`. Остаток: скейл ≥10M (skew/no_filter unit-тестированы), сам Greengage, eval 3.5 (инвариант 8 — live GraceKelly), DataLens 3.1/3.2 (блок: IAM/workbook+HC). **Дальше — только по явному «go».**

**Ранее (2026-06-13): PHASE 3 PREP + S4-РЕШЕНИЯ ВЫПОЛНЕНЫ — по «делай всё на максималках» + «реши сам, как оптимально».** Сделан исчерпывающий prep-документ `docs/plans/2026-06-13-phase3-prep.md`: (A) IR→DataLens capability matrix, обоснованная официальными доками (base `https://api.datalens.tech`, auth `Bearer <IAM>`+`x-dl-org-id`, строковые entryId, RPC createConnection/Dataset/Dashboard[experimental]; **декартова heatmap в DataLens отсутствует → деградация в Pivot+цвет**; joins зашиты в SQL → отдельной обработки не требуют); (B) аудит dialect-шва SQL_GEN под Greengage/PG с точными локациями; (C) ревью BIAdapter Protocol; (D) advisor rule-pack-per-engine seam; (E) introspector + `Physical.distribution_key`. **Два S4-решения приняты по делегации и РЕАЛИЗОВАНЫ (безопасно, без стенда): S4-1 `TargetBI.DATALENS` добавлен (ir/spec.py); S4-2 ref `id: int`→`id: int \| str` (adapters/base.py) — взят `int\|str`, НЕ `str`: ref'ы адаптер-внутренние, поэтому Superset-путь остался байт-в-байт прежним (int на проводе, form_data/native_filters/position_json не тронуты), DataLens положит строковый entryId.** Заодно не-S4: `RunQuery` вынесен в `introspect/base.py` (устранён дубль в clickhouse.py+gaps.py, PG-путь не тянет CH-модуль). ARCHITECTURE §3.5 обновлён. **pytest 219 / ruff / black clean** после правок, поведение не изменилось. Движок-зависимые швы (dialect-param, per-engine EXPLAIN/trial-run, rule-pack-selection, `Physical.distribution_key`) НЕ трогались — нужен live PG-стенд (3.3/3.4). **Реализацию live-части Phase 3 не начинать без разблокировки ресурсов** (IAM-токен/org-id/workbook для DataLens-спайка 3.1; PG-стенд на Mac для Greengage). Последовательность — §G документа. Репо без remote.

**Ранее (2026-06-13): NATIVE DASHBOARD FILTERS реализованы (первая задача Phase 3, по «реши сам»).** Снято предупреждение «фильтры не переносятся» + advisor-F3. `adapters/superset/native_filters.py` компилирует `spec.filters` → `json_metadata.native_filter_configuration` (формат реверснут с живого стенда). **Scope-to-applicable**: фильтр выводится только на чарты, чей grain (`group_columns`) содержит колонку; остальные в `scope.excluded` (KPI «общая выручка» остаётся одним числом под city-фильтром). `filterType` по РОЛИ колонки (time→`filter_time` пустой target; иначе `filter_select` `targets:[{datasetId,column:{name}}]`), не из `DashboardFilter.type` (дефолт «time_range» LLM не переопределяет). **Семантика limit**: чарт в scope теряет top-N `LIMIT` в SQL датасета (`generate_chart_sql(apply_limit=False)`, `participating_chart_ids`) → лимит в form_data `row_limit`; иначе фильтр ре-ранжировал бы пре-обрезанный топ-N + опции select были бы обрезаны. `spec_summary` теперь честно перечисляет, на какие чарты фильтр действует/не действует. `build(spec, model)`/`assemble_dashboard(...datasets,model)` — аддитивно к Protocol. **Проверено живьём**: contract-тест `native_filter_configuration` round-trip PASS; браузер — city-фильтр реально фильтрует bar/line/table, KPI цел (исключён), опции = ВСЕ 20 городов (док-во что LIMIT ушёл из датасета); pytest **219 passed** (+6), live integration **21 passed** (+1), ruff/black clean. Деталь: ARCHITECTURE §3.5, PLAN «Межфазное». **ВЛИТО В MAIN** (`phase-3/native-filters` → main `6a97732`, merge --no-ff; feat `ad40b51`). Репо без remote — push невозможен. **Phase 3 (3.1 DataLens-спайк и далее) не начинать без явного «go».**

**Ранее (2026-06-13): JOINS В IR ВЛИТЫ В MAIN — гейт инварианта 8 пройден.** Снято ограничение Phase 0 (решение «реши сам»): qualified-измерения из смежных таблиц (`"dm.stores.city"`) + явный `query.joins[{table,on_left,on_right}]`; валидация принимает только пары=рёбра semantic model (выдуманные условия отклоняются, незадействованный join — ошибка, коллизии bare-имён — ошибка, меры только с базовой таблицы, multi-hop нет); SQL_GEN: LEFT JOIN, все ссылки квалифицируются базовой таблицей, присоединённые колонки алиасятся в bare-имя (`column_alias()` в ir/spec.py). Деталь: ARCHITECTURE §3.4; PLAN.md «Межфазное». pytest **213 passed**, ruff/black clean; contract-сьют live 20/20.

**Гейт пройден (живьём через GraceKelly/Sonnet + стенд на Mac):** (1) полный golden-eval 26 кейсов — joins-кейс `g12_revenue_by_city_join` **PASS**; первый прогон 22/26, 4 FAIL разобраны: `g3` — флейк (PASS на ретрае), `it1` — инфра-флейк браузерного провайдера GK `unknown_error` (PASS на ретрае), `g7`+`it2` — дефект ожиданий кейсов (писались до joins; с joins LLM законно берёт читаемое `dm.stores.name` через JOIN вместо голого `store_id`) → введён `GoldenCase.expect_columns_any` (группы альтернатив), оба PASS живьём (commit `c772cf5`); порог clear ≥80% выполнен, ambiguous/infeasible все flagged; промпт SPEC_RULES менять не пришлось. (2) live e2e через web UI: «Топ-10 городов по выручке за июнь 2026» → spec с `dm.stores.city` + явными joins → сборка (все 4 чарта прошли EXPLAIN+LIMIT-прогон на живом CH) → дашборд `/superset/dashboard/8/`, 4 чарта (big_number/bar/line с series по city/table) отрисованы с реальными данными (Самара 443M, Ижевск 420M, Омск 415M… — точно совпало с прямым CH-запросом), консоль чистая, скриншот снят. Merge `interphase/ir-joins` → main. **Push невозможен — у репо нет remote.**

Native filters — НЕ делать сейчас, оформлено первой задачей Phase 3 (PLAN.md, межфазная секция, нужен дизайн-степ: grain датасетов + семантика limit). **Phase 3 не начинать без явного «go».**

**Ранее (2026-06-13, позже): S6-РЕВЬЮ PHASE 2 ПРОЙДЕНО, P2 ЗАКРЫТ, ВЛИТО В MAIN.** Ревью субагентом code-reviewer (Fable) → `fable_audit_phase2.md`: **P1 нет, 1×P2 + 7×P3**, вердикт «закрыть P2 → merge». Инварианты 1–8 соблюдены, XSS нет (везде `textContent`), Store потокобезопасен. **F1 [P2] закрыт** (pytest 196): упавшая сборка оставляла сессию в APPROVED → каждый retry 409; теперь approve при `build_status=="failed"` пересобирает тот же approved spec (+ тест `test_failed_build_can_be_retried`). **P3-backlog ревью ПОЛНОСТЬЮ ЗАКРЫТ той же сессией (2026-06-13, pytest 205):** F2 — start до регистрации в реестре (упавший LLM = 502 без зомби, reply не гоняется со start); F3 — кап реестра `MAX_SESSIONS=200` (выселяются старейшие idle, building/locked не трогаются) + `DELETE /sessions/{id}`; F4 — SSE-heartbeat (`: ping` каждые 15s, дисконнект освобождает поток); F5 — middleware режет cross-origin мутации (Origin≠Host → 403; curl/CLI без Origin проходят); F6 — approve перевалидирует spec против живой модели → 409 с понятным текстом; F7 — dbt sources матчатся по `identifier`; F8 — селектор BI disabled с честным tooltip. Также пофикшен **bar-рендер по жалобе пользователя**: числовой dimension на оси X (store_id) рисовался тонкими палками на continuous value-axis — `xAxisForceCategorical` для BAR/STACKED_BAR + сорт топ-N по мере из order_by спеки (ключи сверены с ассетами пиннед 4.1.2, контракт-сьют 18/18 живьём, дашборды 5/6 починены in-place, эталон — дашборд 7). **Phase 3 НЕ начинать без явного «go»**; перед ней — вопрос native filters (deferred F3 Phase 1) и судьба «joins в IR» (в PLAN нет ни в одной фазе, см. наблюдение ниже).

**Ранее (2026-06-13): Phase 2 ЗАВЕРШЕНА, live-смоук ПРОЙДЕН, S6 ОБЪЯВЛЕН** в ветке `phase-2/web-ui` (pytest 195, ruff/black clean). **Live-смоук serve+web UI против реального стенда (Mac через SSH-туннель, GraceKelly/Sonnet) — оба режима e2e**: (1) текст → превью 4 чарта + critical-вердикт advisor с EXPLAIN-evidence → сборка `/superset/dashboard/4/` → итерация словами (bar→pie) → пересборка `/superset/dashboard/5/`, рендер проверен скриншотом; (2) fields-first (drag&drop + клик-фоллбек) → seed_analysis в превью → 4 critical-вердикта (фуллскан 20M, реальный EXPLAIN) → сборка `/superset/dashboard/6/`, скриншот. Консоль чистая. **Находка смоука исправлена**: правка, невыразимая в IR (поле из смежной таблицы — джойнов в IR нет, ограничение ещё Phase 0, в PLAN фаз 1–2 задачи на joins не было), молча возвращала прежний spec с «Предлагаю вариант» — теперь детерминированная noop-детекция в `machine.py` (AgentTurn.noop) + честное сообщение в UI; noop-ветка проверена юнит-тестом и браузером на dev-стенде (вторая правка в `dev_ui_server.py` скриптована как noop). **Известное поведение LLM** (наблюдение, промпты не трогали): на join-запрос («выручка по городам») может выкрутиться одно-табличным суррогатом (count магазинов по city с заголовком про выручку) — превью честно показывает реальные поля, снимается реализацией joins в IR. **Merge в main — после ревью фазы (протокол S6); Phase 3 не начинать.** Открытый вопрос владельцу: native filters в Superset-адаптере (deferred F3 + предупреждение «фильтры дашборда не переносятся») — делать в начале Phase 3 или отдельной задачей до неё. Детали по задачам — ниже.

**Phase 2 НАЧАТА** (2026-06-12, Fable, по «продолжи» после закрытия S6): Phase 1 влита в `main` (merge `a6f2755`), работа в ветке `phase-2/web-ui`. **2.1 HTTP API реализовано**: `auto_bi/api/` (create_app c DI, SessionManager c per-session lock и буфером событий), endpoints `/api/v1/sessions[…]` + SSE `/events`, `auto_bi serve` (uvicorn-wiring), Store потокобезопасен (lock + check_same_thread=False), контракт «правка не теряет сессию» перенесён в HTTP (200+error). **2.5 dm_change_request реализовано**: `auto_bi/dmcr.py` (детерминированный markdown-рендер заявки владельцу DM из store-строки + контекст сессии через JOIN), API `GET/PATCH /api/v1/dm-change-requests[...]` (список по статусу, заявка с markdown, lifecycle open→submitted/accepted/rejected).

**2.4 итерации реализованы**: APPROVED не терминальна — правка словами после сборки патчит spec и возвращает в APPROVE, следующий approve пересобирает; история spec'ов в store append-only; API re-approve сбрасывает SSE-буфер (стрим = одна сборка); CLI-чат после сборки предлагает доработку. In-place обновление существующего дашборда (вместо нового) отложено до живого стенда (нужен update-путь адаптера + contract-тесты).

**2.2 web UI реализован** (`auto_bi/api/static/`, vanilla HTML/CSS/JS без сборочной цепочки, отдаётся из FastAPI на `/`): чат (clarify-вопросы, ошибки правок), превью spec карточками с вердиктами advisor (accent-полоса по severity) и предупреждением о фильтрах, кнопка «Собрать/Пересобрать», SSE-лог сборки, ссылка на дашборд, список dm_change_requests. Спокойный белый layout, тёплые нейтралы, один акцент (teal), system-шрифты. **Проверен в браузере** (Playwright, dev-стенд `scripts/dev_ui_server.py` со скриптованным LLM): clarify → spec → build → правка → rebuild, консоль чистая. pytest **168 passed**, ruff/black clean. Деталь: ARCHITECTURE §3.7.

**2.3 fields-first реализовано** (2026-06-13, Fable — S2-задача, модель допустимая): `auto_bi/agent/seed.py` (FieldsSeed → детерминированная валидация по модели → текстовый рендер для GROUNDING, pinned-таблицы в селекции), `GET /api/v1/model/fields` + seed в `POST /sessions` (невалидное поле = 422), UI-вкладка «Полями» (drag&drop + клик-фоллбек, группы-черновики чартов). Вместо «вариантов дашборда» — один spec + детерминированный `seed_analysis` (D5; `AgentTurn.notes` → превью UI). Шаблоны промптов НЕ менялись; live-гейт: golden f1/f2 (fields-first) + спот-чек g1/a1 через живой GraceKelly — **4/4 PASS**. Браузерная проверка Playwright на `scripts/dev_ui_server.py`: drag&drop → clarify → превью с анализом → сборка, консоль чистая. pytest **181 passed**, ruff/black clean. План: `phase2-fields-first.md`; деталь: ARCHITECTURE §3.7.

**2.6 dbt-импорт реализовано** (2026-06-13, Fable): `semantic/dbt_import.py` + CLI `auto_bi dbt-import --manifest … [--catalog …] [--dry-run]`. dbt = enrichment, НЕ источник схемы: fill-empty-only (ручные правки выигрывают), relationships→joins+fk с дедупом, unmatched репортятся без добавления, идемпотентно. Деталь: ARCHITECTURE §3.1.

**2.7 enrichment UI реализовано** (2026-06-13, Fable): `GET /api/v1/model/gaps` (offline find_gaps) + `PATCH /api/v1/model/tables/{t}[/columns/{c}]` (описания, role/agg с валидацией F9-зеркала; запись в model.yaml включается `create_app(model_path=…)`, без него 503) + секция «Качество модели» в web UI с инлайн-редакторами в findings. Проверено Playwright'ом на dev-стенде: правка убирает finding live, yaml на диске обновляется, консоль чистая. pytest **193 passed**. Деталь: ARCHITECTURE §3.7.

**2.8 eval до 25 ЗАКРЫТО** (2026-06-13, Fable): GOLDEN_CASES=25 — 11 clear + 4 итерационных (`edit`/`edit_expect_*` в GoldenCase, runner применяет правку после clear-проверок и проверяет ПРОПАТЧЕННЫЙ spec) + 3 fields-first + 4 ambiguous + 4 infeasible. **Полный живой прогон через GraceKelly/Sonnet: 23/25, пороги выполнены (clear 88% ≥ 80%, flagged все)**; оба FAIL разобраны: f3 — инфра-флейк браузерного провайдера GK (PASS на ретрае), it2 — дефект кейса («убери чарт» в ед. числе при двух top-чартах в спеке; LLM вёл себя корректно) — правка кейса со явным scope, PASS живьём. Промпты агента не менялись.

**Все задачи Phase 2 (2.1–2.8) реализованы, live-смоук пройден 2026-06-13 (см. ТЕКУЩЕЕ), S6 объявлен.** Exit criteria: оба режима собраны/доработаны через web UI живьём (дашборды 4/5/6 на стенде, скриншоты); fields-first дал spec того же качества (golden f1–f3 + live 4/4 ещё в 2.3); dm_change_request по critical-вердикту — покрыт тестами/eval и dev-стендом (на живом демо-DM critical'ы корректно классифицируются как spec_adjustment — DM-уровневых дефектов в демо нет by design, живые DCR-кандидаты были на X5 в 1.10).

**Phase 0 ЗАВЕРШЕНА и верифицирована** (2026-06-11, ветка `phase-0/vertical-slice`). Exit criteria проверены реальными прогонами: стенд на Mac (`~/auto_bi_stand`, compose: Superset 4.1.2 + CH 24.8, демо-DM 20M строк), интроспекция → `semantic/model.yaml` закоммичен, 4 contract-теста form_data прошли против живого Superset, e2e `auto_bi build "Обзор продаж: …"` собрал дашборд из 3 чартов (`/superset/dashboard/1/`), GraceKelly-вызовы в `logs/llm_calls.jsonl`. Доступ к стенду с Windows — SSH-туннель `ssh -N -L 8123:localhost:8123 -L 8088:localhost:8088 deproject-mac`; Docker ТОЛЬКО на Mac.

Известное ограничение Phase 0 (by design): джойнов в IR нет — запросы с полями из смежных таблиц («топ городов» при city в dm.stores) отклоняются валидацией; промпт предупреждает LLM. Снимается в Phase 1/2 по PLAN.

**S6 ЗАКРЫТ** (2026-06-12, решение пользователя): внешнее ревью заменено аудитом Fable (`fable_audit.md`), все 8 findings исправлены и верифицированы. Phase 0 смержена в `main` (`3f99f35`).

**Phase 1 ПОЧТИ ЗАКРЫТА** (2026-06-12, Fable, ветка `phase-1/mvp-superset-advisor`). Сделано ВСЁ кроме 1.10: **1.1** IR 9 viz (`ad1999d`), **1.2** form_data все 9 viz — 18 integration-тестов против живого Superset 4.1.2 + визуальная проверка рендера всех 6 новых в Explore (`273663b`), **1.3** layout (`d8ada46`), **1.4** state machine GROUNDING→CLARIFY*(вопросы детерминированно только из report, ≤3/раунд, ≤2 раундов)→PROPOSE→APPROVE+правки словами (`77e3149`), **1.5** context selection <40k (стем-скоринг+жадная упаковка, `9f2bf5e`), **1.6** Advisor (`3dad971`), **1.7** нарратив advisor (вердикт решает код, LLM формулирует; `77e3149`), **1.8** `auto_bi chat` (rich), **1.9** SQLite store (`ab82b88`), **1.11** eval-сьют `auto_bi eval` (`9669e56`), **1.12** reasoning-политика `llm/policy.py`. pytest **131 passed / 18 deselected (integration)**.

**Exit criteria Phase 1 проверены живыми прогонами** (2026-06-12): advisor-сьют **9/9** (6 подсаженных анти-паттернов с правильными правилами + 3 clean без false positives); golden-сьют через живой GraceKelly/Sonnet: **clear 9/9 (0 лишних вопросов), ambiguous 3/3, infeasible 3/3** (единичные FAIL в прогонах — инфра-флейки браузерного провайдера GraceKelly, PASS на ретрае; промпт-фиксы по итогам прогонов: конвенция имён колонок `cbc4727`/`07a487b`, repair-промпт со схемой `44a7e39`); **e2e диалог живьём**: запрос с анти-паттерном → critical-вердикт advisor (оба правила, EXPLAIN-evidence) → правка «ограничь июнем 2026» → вердикты исчезли → approve → дашборд `/superset/dashboard/2/` собран и проверен скриншотом; store записал messages/specs(proposed→approved)/builds/llm_calls.

**Phase 1 ЗАКРЫТА ПОЛНОСТЬЮ, S6 ОБЪЯВЛЕН** (2026-06-12, Fable). **1.10 выполнено**: реальный DWH = DV2/X5 Retail Hero из DE_project, срез 15.05M line items (покупки ≥2019-02-15) / 2.6M заказов / 400k клиентов / 43k товаров загружен в CH стенда (БД `rv` + `marts`; Lima-кластер с оригиналом не совместим со стендом на 8GB iMac). Марты DE_project материализованы «как есть» (branch_pnl / customer_360 / returns_velocity, tax rates 0.20/0.05/0.12 сошлись). Доступ Auto_BI — только `auto_bi_ro` (GRANT SELECT на rv/marts). Интроспекция → `semantic/model_x5.yaml` (3 таблицы, 37 колонок; реальный DWH выловил 2 бага интроспектора на Nullable(Nothing) — исправлены). Новая команда **`auto_bi gaps`** (детерминированный аудит DM: описания, изоляция, all-NULL/constant колонки, прёагрегированная грануляция) → `docs/gaps_report_marts_x5.md`: **18 findings (1 critical / 14 warn / 3 info), 10 кандидатов в dm_change_request**. Живой кейс по реальному DM: 4 чарта, advisor с реальным EXPLAIN-evidence (scan 100%, GROUP BY 401k), дашборд `/superset/dashboard/3/` (скриншот). pytest **140 passed**. Стенд-скрипты: `scripts/stand_load_bv_mat.sh`, `scripts/stand_create_marts.sql`; runbook `docs/plans/2026-06-12-1.10-real-dwh-x5-runbook.md`. Демо-`dm` не тронута. **Phase 2 НЕ начинать** (протокол S6): сначала ревью фазы (например `/cxkm`) по решению пользователя. Естественное продолжение после ревью: достройка DM по gaps report (fact_order_lines + dims из rv) через dm_change_request-workflow. Push невозможен — у репо нет remote.

> 2026-06-12 (позже): **S6-ревью Phase 1 ПРОЙДЕНО** — по решению пользователя («запроси ревью у субагента») проведено субагентом code-reviewer (Fable), отчёт `fable_audit_phase1.md`: P1 нет, 2×P2 + 8×P3, вердикт «Phase 2 открывать можно, P2 закрыть до начала». **Все findings закрыты той же сессией** (план `phase1-audit-fixes.md`): F1 patch_spec — pinned-таблицы spec + скоринг по spec-полям + бюджет учитывает spec JSON; F2 — dashboard-фильтры объявляются в превью approve (spec_summary), не молча дропаются; F4 — joins/metrics в бюджете селекции, over-budget mandatory-таблица обрезает колонки по релевантности вместо LLMError; F5 — дедуп dm_change_request по (table, rule) в сессии; F6 — ошибка правки не убивает chat-сессию, GraceKellyClient один на REPL; F7 — JSON Schema в VALIDATION_FEEDBACK_PROMPT; F8 — gaps: экранирование backtick-идентификаторов, обе недельные конвенции (Sun/Mon), упавшая проба деградирует в INFO-finding, дедуп column_all_null; F9 — sum/avg/min/max только на role=measure; F10 — PRAGMA foreign_keys=ON + user_version=1. F3 (advisor не видит dashboard-фильтры) задокументирован как coupled с native filters → Phase 2 (docstring advisor/clickhouse.py); deviation 1.6 (point_lookup_pattern отложен) зафиксирован в PLAN.md. pytest **155 passed / 18 deselected**, ruff/black clean. Live-прогоны не перепроверялись (стенд недоступен) — exit criteria Phase 1 приняты по документации. **Phase 2 можно открывать по команде пользователя.**

> 2026-06-12: `/cxkm` по диффу `main...HEAD` запущен — оба внешних ревьюера недоступны (CX `codex app-server exited unexpectedly`; KM/mco kimi `LLM not set` — провайдер не сконфигурирован). Postflight локально зелёный: `ruff` clean, `pytest` 55 passed / 4 deselected (live-тесты гейтятся стендом). S6-ревью НЕ пройдено → Phase 1 не начинать. Re-run-материалы (`codex-prompt-phase0-review.md`, `.tmp/`) удалены после того, как ревью прошло субагентом (см. заметку выше). Hygiene: `.gitignore` теперь покрывает `.env.*` (коммит `5836695`).

> 2026-06-12: внешнее ревью Phase 0 проведено локальной моделью (Fable) → 8 findings в `fable_audit.md` (3×P2 + 5×P3). **Все закрыты в коде той же сессией** (по явному запросу пользователя): F1 SQL-инъекция через `measure.label` в form_data (экранирование), F2 `order_by` по мере → невалидный CH-SQL (маппинг на алиас + `measure_alias` в `ir/spec.py`), F3 мёртвый `AUTO_BI_SEND_SAMPLES` (проброшен в `render_model`, ARCHITECTURE §4 уточнён), + P3: пустой `IN`, connect-retries клиентов, ранний выход repair-петель, уникальность имени dataset, defensive `validate_spec` в pipeline. `ruff`/`black` clean, `pytest` **65 passed / 4 deselected**. Закоммичено в ветку: `f7a6780`. **Live-смоук P2 против работающего CH 24.8 стенда (Mac) пройден**: F2-fixed `ORDER BY "sum_revenue"` → 5 строк rc=0; F2-buggy контроль `ORDER BY "revenue"` → `Code 215 NOT_AN_AGGREGATE` (баг подтверждён, фикс снимает); F1 escaped-label → SUM факта (236e9), не `system.numbers` (инъекции нет). **S6 по-прежнему открыт** — внешнее CX/KM-ревью не пройдено; Phase 1 не начинать.

## Скоуп (решение 2026-06-11)

RU-рынок. **v1 = ClickHouse (DM) + Superset (BI)**; v2 = Greengage/Greenplum + DataLens (Public API). Power BI / Tableau / Metabase — вне скоупа. Универсальность держим в швах (IR, `BIAdapter`, rule pack per engine), имплементируем один путь. Демо-DM — на ClickHouse (docker).

## Инварианты дизайна (не нарушать без обновления ARCHITECTURE.md)

1. **IR-first**: LLM генерирует только `DashboardSpec` (pydantic IR). Нативные форматы BI (form_data, конфиги DataLens) — только детерминированный код адаптеров.
2. Spec валидируется против `semantic/model.yaml` ДО любых вызовов BI; неизвестное поле → reject + repair loop (max 3), никаких молчаливых починок.
3. SQL: только SELECT (sqlglot-guard), EXPLAIN + LIMIT-прогон, read-only роль DWH.
4. Уточняющие вопросы — только из grounding report, ≤3 за раунд; однозначный запрос → ноль вопросов.
5. **Feasibility Advisor**: вердикты только из детерминированных findings (ключи/статистика/EXPLAIN) — LLM формулирует, но не «решает»; advisory-only, сборку никогда не блокирует; классы `ok / spec_adjustment / dm_change_request`.
6. **Fields-first** (drag&drop полей) — второй вход в тот же пайплайн (seed для GROUNDING), не отдельный конструктор чартов.
7. Версия Superset запинена в docker-compose; обновление — отдельная задача с прогоном contract-тестов.
8. Изменения промптов мержатся только с прогоном eval-сьюта (с Phase 1).

## LLM

GraceKelly (локальный сервис, должен быть запущен): `POST http://127.0.0.1:8011/api/v1/orchestrate`
`{"prompt": ..., "model": "claude-sonnet-4-6", "reasoning": true, "decompose": false, "session_id": ..., "metadata": {"app": "auto_bi"}}`
- `prompt` ≤ 40 000 символов → context selection обязателен на больших DM.
- Text-in/text-out: структурированный вывод через JSON-блок → pydantic → repair loop.
- Все вызовы через `llm/LLMClient`-абстракцию, напрямую httpx из бизнес-кода не дёргать.
- `reasoning: true` — на GROUNDING/PROPOSE_SPEC; механические шаги — без reasoning.

## Stack и конвенции

- Python 3.12 + uv; ruff + black; pytest. FastAPI, pydantic v2, sqlglot, httpx.
- Без LangChain/LangGraph — агент = простая state machine.
- Код/идентификаторы — английский; доки и общение — русский.
- Секреты только в `.env` (в `.gitignore`); в логи/доки не попадают.
- Локальный стенд: `docker-compose up` (Superset + ClickHouse с демо-DM).

## Стопперы переключения модели (/auto)

Дефолт реализации — **Opus high**. Агент не может сменить модель сам, поэтому стоппер = остановка с протоколом (ниже). Триггеры объективные, «по ощущениям» не останавливаться.

| Код | Триггер (проверяемый) | Действие |
|---|---|---|
| S1 дебаг-цикл | Один и тот же баг/тест: 3 цикла «гипотеза → правка → прогон» без смены симптома (новая ошибка = прогресс, счётчик сбрасывается) | STOP → рекомендация **Fable**; можно взять независимую задачу фазы |
| S2 промпты/eval | Задачи промпт-инжиниринга агента и дизайна eval (в Phase 1 это 1.4–1.7: GROUNDING/CLARIFY/PROPOSE_SPEC, политика уточнений, golden-кейсы) | **Не начинать на Opus в /auto.** STOP до старта → Fable или ручная сессия |
| S3 eval-регрессия | После правки промптов eval ниже базовой на ≥10 п.п. или ниже exit-порога фазы, и 1 итерация починки не вернула | STOP → Fable; промпт-изменение не мержить |
| S4 инварианты | Решение требует менять IR-схему, интерфейс `BIAdapter` или инварианты 1–8 этого файла | STOP всегда — вопрос пользователю, модель не важна |
| S5 form_data | viz_type не проходит contract-тест после 3 вариантов шаблона | STOP → приложить diff «create vs GET»; ручная сессия (вероятно с браузером), модель вторична |
| S6 граница фазы | Exit criteria фазы выполнены и проверены | STOP → предложить `/cxkm`-ревью перед следующей фазой; следующую фазу самому не начинать |
| S7 инфра | GraceKelly / Superset / ClickHouse-стенд недоступен после 2 попыток восстановления | STOP **без** эскалации модели — это не модельная проблема |

**Протокол STOP:** (1) WIP-коммит в текущую ветку `wip(stopper:Sx): <задача>`; (2) одна строка в блок «Статус» этого файла: дата, задача, стоппер, рекомендованная модель; (3) финальное сообщение: причина, что переключить, точка продолжения. После S1/S3/S5 допустимо взять независимую задачу той же фазы; после S2/S4/S6 — только остановка.

## Definition of done (фаза/задача)

- Тесты зелёные (pytest), ruff чистый.
- Exit criteria фазы из PLAN.md проверены реальными прогонами, не «на глаз».
- Обновлены: статус в этом файле; ARCHITECTURE.md — если дизайн уточнился.
