# Auto_BI — план до 9.8/10 (рабочий трекер)

> **Остаток работы теперь трекается в `new_plen_05_07_26.md` — этот файл история волн 1–2.**
>
> **Статус: 12 из 17 audit steps (S01–S10/S12/S14 done, S11/S13/S15–S18 open/gated). Main=`3beb7e2`, pytest 705, cov 95%. S01 ЗАКРЫТ (2026-07-06, live-хвост зелёный + merge ff в main), S02–S10/S12/S14 закрыты, S11 механизм готов, S13-Superset+Трек A смёржены.**
> Источник находок волн 1–2: `audit_fable_03_07_26.md` (аудит Fable, 2026-07-03, main=`f94cec5`).
> Внутренний документ, НЕ в публичный репо. Не путать с `docs/PLAN.md` (история фаз Phase 0–4, публичный).

Гейты по дисциплине проекта: 🟡 S2 (промпт+eval) = Fable-сессия · 🟠 S4 (IR-контракт) = design-doc + «go» владельца · 🔵 стенд (Mac) · 🟣 владелец (продуктовое решение) · 🔴 наружу (деплой/ключи/деньги). Всё безгейтовое — автономно (зелёный гейт + verify → merge+push).

---

## Definition of Done — что такое 9.8/10 здесь

1. **Воспроизводимость**: сторонний инженер с ClickHouse+Superset и Anthropic-ключом доходит от `git clone` (или `docker run`) до собранного дашборда за ≤30 минут строго по README, без автора рядом.
2. **Полнота обещания**: text-first выражает ВСЁ аналитическое ядро IR (ratio/grain/yoy/lag/Pareto/histogram) с golden-eval-подтверждением.
3. **Дистрибуция**: semver-релизы + CHANGELOG + собираемый в CI и опубликованный Docker-образ.
4. **CI-полнота**: unit + integration (живые CH+Superset в Actions) + advisor-eval + golden-replay на каждый PR.
5. **Прод-готовность**: security-hardening (cookie/rate-limit/токены), readiness, deployment-гайд, LLM-квоты.
6. **Витрина**: живое публичное демо ИЛИ (минимум) видео/GIF полного цикла в README; README без дрейфа цифр.
7. **Рендер без известных MED-багов**: C7 закрыт; C2 закрыт или честно задокументирован как engine-limit.
8. Финальный адверсариальный аудит после всех правок — 0 HIGH / 0 MED открытых.

---

## Шаги

- [x] ~~**S01 — F1: аналитическое ядро в text-first**~~ · 🟡 S2 · **Fable** · L · зависит: — · **ГОТОВО (2026-07-06, main=`3beb7e2`, ff-merge, CI зелёный)**
  ~~Rebase на main (+29 коммитов), офлайн-гейт, live-хвост CH+GP полным списком, merge.~~ — СДЕЛАНО: ветка `feat/s01-text-first-core` rebased на main `e36e435` (2 конфликта — `docs/ARCHITECTURE.md`/`tests/test_eval.py`, оба чисто аддитивные, разрешены сохранением обеих сторон). Офлайн-гейт: ruff/black · mypy 0/69 · pytest **705**/1 skipped/34 deselected, 95% cov · advisor 9/9. **Live-хвост (GraceKelly→Perplexity Claude Sonnet 5.0, thinking) — ВСЕ 22 запрошенных кейса PASS**: CH-ядро `g3_top_stores`/`g19_lag3_pct`/`g20_running_total`/`i1_salaries`/`i2_returns`/`i4_weather` (6/6) + GP-сет `gp_g1`–`gp_g12`/`gp_it1`/`gp_i1`–`gp_i3` (16/16, `gp_a1` исключён ранее). Несколько транзиентных транспортных сбоев (браузер-таймаут, временный model-selection «Sonar» вместо «Claude Sonnet 5.0», 2 внешних рестарта GK) — все разрешились одним повтором, ни один кейс не дошёл до 3 попыток на одном симптоме (S2-стоппер не понадобился). `GRACEKELLY_BROWSER_CALL_TIMEOUT_SECONDS=240` (вместо 120) снял историческую флейковость g19/g20. Merge: чистый ff (`git merge --ff-only`, без merge-коммита) → main=`3beb7e2` → `git push origin main`. Рebased-копия ветки также запушена под `feat/s01-text-first-core-rebased` (force-push избежан, т.к. rebase переписал историю). Закрывает P-1. **Критический путь — открывает P2 (S11)/P7 (S16)/рекомендацию v0.2.0.**
  **⏸ ОТЛОЖЕНО (владелец, 2026-07-04): добить live-хвост golden на ОСНОВНОМ пути GraceKelly→Sonnet 5.0 ЧЕРЕЗ ДЕНЬ, когда восстановится лимит Perplexity Pro** — кейсы g3/g19/g20 (флейки) + i1/i2/i4 + GP-сет (16, `--model-path semantic/model_gp.yaml`) → зелено → ff-push `HEAD:main` (ветка уже rebased, конфликтов нет). GK: `cd D:\GraceKelly && .venv\Scripts\python.exe -m uvicorn gracekelly.main:create_app --factory --host 127.0.0.1 --port 8011`; `.env` worktree = `AUTO_BI_GRACEKELLY_MODEL=claude-sonnet-5-0` (+thinking-тумблер жмётся адаптером сам).
  **Сессия 2026-07-04 (Opus): rebased + кросс-проверка на Mistral (квоты Perplexity не было).** Ветку rebased на `origin/main` (S02–S06; 0 конфликтов) → офлайн-гейт зелёный (ruff/black · mypy 0/67 · pytest 610 · advisor 9/9). По вопросу владельца «проверял ли на Mistral» реализован Mistral-провайдер (S18, ниже) и прогнан живой срез S01-golden через `mistral-large-latest`: **12/14** — ratio(g14)/grain(g15)/yoy(g17)/running_total(g20)/share(g21)/Парето(g22)/histogram(g23) ✓, ambiguous a1/a2/a4 ✓, infeasible i3 ✓; ✗ только g18 mom + g19 lag-N (Mistral-large генерит невалидный spec, детали вариативны = слабость модели, НЕ дефект промптов — Sonnet их проходит). Вывод: S01-промпты кросс-модельно робастны; Mistral слаб на 2 сложнейших примитивах → владелец «не используем как fallback».
  **Сессия 2026-07-04 (Fable):** гоча GK — `claude-sonnet-5-0` = меню «Claude Sonnet 5.0» + ОБЯЗАТЕЛЬНЫЙ thinking-тумблер (адаптер жмёт сам; не нашёл → provider_unavailable ×3 → circuit breaker — так умер вчерашний прогон). Под thinking ambiguous-семья СТАБИЛЬНО регрессировала (модель «разумно выбирает» вместо вопроса) → промпт-итерация `c9a310e`: правило 3 (период-литерал = фильтр, не сущность), правило 5 (контрпример «Выручка по названию» → ambiguous), правило 6 (несколько мер под расплывчатым словом = ВСЕГДА ambiguous, few-shot JSON; явное имя колонки снимает). gp_a1 выведен из GP-сета (колонка буквально qty — кейс нечестный; CLARIFY покрыт CH a1/a2/a4), g3 ослаблен `expect_columns_any` (имя через join легитимно). Офлайн-гейт: ruff/black/mypy 0/66 · pytest 599 · advisor 9/9. **Live под ФИНАЛЬНЫМ промптом: 34/37 CH PASS** (все 23 g-кейса вкл. ядро, it1–4, f1–3, a1/a2/a4 стабильно спрашивают, i3); контроль-шестёрка 6/6. **Остаток до merge: g3+g19+g20 (флейки: spec-джойн 1 из 3 / таймауты 120с) + i1/i2/i4 (финальным промптом не гнались) + GP-сет** — ⚠️ ТОЛЬКО по слову владельца, квота Perplexity (см. заметку внизу). Merge: rebase на origin/main (там уже S02–S06!) → гейт → ff-push.
  ~~SPEC_RULES (`agent/propose.py`) дополнить transform/denominator/time_grain/lag_periods/bins с примерами; +10–15 golden-кейсов («средний чек», «по месяцам», «г/г», «Парето», «распределение»); прогон golden на живом LLM; advisor-eval 9/9 не трогать~~ — СДЕЛАНО: SPEC_RULES п.6–9 (few-shot, дословно по validate.py) + grounding правила 2/5 (ratio теперь matched; была активная блокировка «отношения дашборд не умеет») + 12 CH-кейсов (g13–g23, it4) + 4 GP-зеркала + expect_transforms/ratio/grain/bins/lag в eval + drift-гарды. Гейт: mypy 0/66 · pytest 599 · advisor 9/9. Live (GK, **claude-sonnet-5-0** — Perplexity-меню сменилось, 4-6 умер → 422, .env переведён): **все 12 новых кейсов ядра PASS**, f1–f3/a1–a2/i1–i3 PASS. **Остался хвост до merge:** один чистый прогон a4+gp_a1 (флейк «количество» на sonnet-5-0 — 1 из 2 прогонов не спрашивает), i4/gp_g11/gp_g12/gp_i1 (убиты остановкой стенда, не поведение), g5_pivot (префлейк НОВОЙ модели на старом кейсе — разобрать отдельно, к S01 не относится). Закрывает P-1. **Критический путь.**

- [x] ~~**S02 — Anthropic — первоклассный путь**~~ · 🟣 (слово владельца на смену дефолта) · Sonnet · S · зависит: — · **ГОТОВО (2026-07-04)**
  ~~Дефолт `llm_provider` → anthropic (GraceKelly — документированная опция); README-шапка + USER_GUIDE §2 quickstart с ключом; попутно фикс B-5 (cli.py:478 печатает «через GraceKelly» при любом провайдере) и B-9 (store в eval-llm). Закрывает P-2.~~ — СДЕЛАНО: дефолт `llm_provider` gracekelly→anthropic (`config.py`); README-шапка + USER_GUIDE §1/§2/§3/§6 + ARCHITECTURE §3.6/ADR D3 + `.env.example` переписаны под новый дефолт (GraceKelly — документированная опция). B-5 закрыт (`cli.py` golden-eval баннер провайдер-aware вместо жёсткого «через GraceKelly»). B-9 закрыт (`_eval` пробрасывает `Store` в `make_llm` — golden-прогоны теперь в `llm_calls`). Гейт: ruff/black/mypy 0/66 · pytest 592/1 skipped (нет `anthropic`-extra в окружении, ожидаемо) / 32 deselected · advisor-eval 9/9. Работа велась в изолированной git-worktree от `origin/main` (не трогала незакоммиченный live-хвост S01 здесь). Коммиты `bc6b708`(feat)+`6a01226`(docs), смержено ff в main + запушено, CI зелёный (run 28684782667). Закрывает P-2.

- [x] ~~**S03 — Doc-sync**~~ · — · Sonnet · S · зависит: S02 (текст про провайдера), S10 (бейдж; до него — заглушка) · **ГОТОВО (2026-07-04)**
  ~~README: 592/95%, бейдж из CI, убрать дрейф; `.env.example` — полный (GP/DataLens/LLM/AUTH/STORE); сверка USER_GUIDE §6 с config.py. Закрывает D-3, D-4.~~ — СДЕЛАНО: README бейдж 93%→95%, «369 unit/API-тестов»→«592 unit/API-теста» (перемерено `uv run --with duckdb --with pytest-cov pytest --cov=auto_bi`: 592 passed/1 skipped(нет anthropic-extra)/32 deselected, 95% coverage), команда разработки в README дополнена `--with duckdb` (иначе локально 580, а не 592 — расхождение с CI без флага). `.env.example` дополнен GP-блоком (6 полей), DataLens-блоком (5 полей + workbook_id + ch_host_from_datalens), ANTHROPIC_MODEL/MAX_TOKENS, STORE_PATH, AUTH-блоком (5 полей) — теперь 1:1 с `config.py::Settings`. USER_GUIDE §6 сверен с `config.py` полe-в-поле — уже был в синхроне, дрейф был только в `.env.example`. Бейдж CI (S10) — по-прежнему статический placeholder до S10, как и предполагал план. Закрывает D-3, D-4. Гейт: docs-only, ruff/black/mypy/pytest не затронуты (перепроверила `ruff check .` — чисто). ⚠️ Найдено по пути: S02 в этом трекере значился `[ ]`, хотя уже был смёржен в main (`bc6b708`/`6a01226`) — статус поправлен отдельно (см. выше), трекер был не обновлён сессией, которая делала S02.

- [x] ~~**S04 — Repo-гигиена**~~ · 🟣 только по cont.8 · Haiku (механика) + Sonnet (dependabot-ревью) · S · зависит: — · **ГОТОВО (2026-07-04)**
  ~~`fable_audit*/phase*` → `docs/history/`; CLAUDE.md сократить до статичного; удалить 38 смерженных локальных веток; смержить 3 dependabot-PR (по одному, с CI); судьба `wip/cont8` — вопрос владельцу. Закрывает D-5, D-6, D-8.~~ — СДЕЛАНО: спросила владельца про `wip/cont8-dashboard-heatmap` (ветка на 45 файлов/2260 удалений устарела от main) — решение «оставить как есть», не трогала. 14 tracked `fable_audit*`/`phase*-*.md` перенесены в `docs/history/` (+ 2 прозы-ссылки на них в ARCHITECTURE.md/PLAN.md обновлены на новый путь); `CLAUDE.md` `## Статус` сокращён до текущей записи, вся история cont.5–cont.16/Phase 0-1 вынесена в `docs/history/claude-status-log.md`. Удалены 41 смерженная локальная ветка (было 42 стейл-ветки в `git branch --merged main`, `docs/s03-doc-sync` — активная ветка параллельной сессии — не тронута, `wip/cont8`/`feat/s01-text-first-core` тоже). Смержены все 3 dependabot-PR по одному (`gh pr merge --squash`): #1 setup-uv v5→v7, #2 checkout v4→v7, #5 python-deps group (anthropic/clickhouse-connect/fastapi/pydantic-settings/sqlglot/pytest/ruff, 7 апдейтов) — каждый с зелёным CI на своём PR, финальный `push`-прогон на main (`4c3af71`, все 3 бампа вместе) тоже зелёный. Коммит `8cc971e` (docs-only, hygiene) + 3 squash-merge коммита. ⚠️ Сессия шла параллельно с другой сессией, активно редактировавшей тот же working tree (`docs/s03-doc-sync`, S06 rate-limit в процессе) — коммитила точечно (`git add <files>`, не `-A`), не трогала её незакоммиченные правки. Закрывает D-5, D-6, D-8.

- [x] ~~**S05 — Community-файлы**~~ · — · Haiku · S · зависит: — · **ГОТОВО (2026-07-04)**
  ~~SECURITY.md (из ARCHITECTURE §4), CONTRIBUTING.md (гейты dev-цикла), issue-шаблоны. Закрывает D-7.~~ — СДЕЛАНО: `SECURITY.md` (модель угроз из ARCHITECTURE §4 + приватный репорт через GitHub Security Advisories + честный список открытых hardening-гэпов B-2/B-3/B-4, закрываются в S06). `CONTRIBUTING.md` (dev-setup, полный локальный гейт зеркалит `ci.yml`, инварианты 1–8 со ссылкой на CLAUDE.md/ARCHITECTURE, golden-eval обязателен при правке промптов, коммит/PR-конвенции). `.github/ISSUE_TEMPLATE/{bug_report.md,feature_request.md,config.yml}` (config.yml форвардит security-репорты на Advisories, не на публичный issue). Docs-only, гейт не требуется (нет кода). Закрывает D-7.

- [x] ~~**S06 — Security-hardening**~~ · — · Sonnet · M · зависит: — · **ГОТОВО (2026-07-04)**
  ~~`secure`-флаг куки (конфиг); rate-limit на login (5/мин/IP + бэкофф); токены → sha256-хэш в store (миграция v6, идемпотентная); периодический purge протухших токенов. Закрывает B-2, B-3, B-4.~~ — СДЕЛАНО: **B-2** `create_app(cookie_secure=...)` — `response.set_cookie(..., secure=cookie_secure)`; `cli.py::_serve` выводит флаг из нового `AUTO_BI_AUTH_COOKIE_SECURE` (tri-state, `None`=авто), авто = `Secure` вкл. везде, кроме bind-хоста `127.0.0.1`/`localhost`/`::1`. **B-3** новый `api/ratelimit.py::LoginRateLimiter` — скользящее окно 5/60с на IP-ключ (`request.client.host`), при превышении 429 + `Retry-After`; лок-аут растёт экспоненциально на каждое повторное нарушение, пока залокано (30с→60с→120с…, потолок 900с), strikes не сбрасываются. Инжектируемые часы (`clock` в конструкторе) — юнит-тесты детерминированы без реального sleep. **B-4** `store/db.py`: `auth_tokens.token` теперь хранит `sha256(raw)` hex, не сырой токен; `create_token`/`token_user`/`delete_token` хэшируют на входе (внешний контракт не поменялся — вызывающий код видит сырой токен как раньше). Миграция v6 (`_SCHEMA_VERSION=6`) переписывает существующие plaintext-токены в hex-хэш, гейтится по форме значения (`_TOKEN_HASH_RE`, не только по версии) — повторный вход в ветку не может хэшировать уже хэшированное. **Периодический purge** — `cli.py::_start_token_purge_thread` (daemon-поток, раз в час, только при `auth_enabled`) вызывает уже существовавший (но нигде не вызывавшийся) `Store.purge_expired_tokens()`. Гейт: ruff/black/mypy 0/67 (новый `api/ratelimit.py`) · pytest 603 passed/1 skipped(нет `anthropic`-extra, ожидаемо)/32 deselected, 95% coverage (+11: 7 `test_ratelimit.py` юнит + 2 `test_api_auth.py` интеграционных 429/Secure-cookie + 1 `test_store.py` v5→v6 миграция-хэширование + 1 `test_auth.py` "хранится как хэш") · advisor-eval 9/9. Доки: ARCHITECTURE §4 (новый абзац B-2/B-3/B-4), SECURITY.md (раздел "Known Hardening Gaps" → "Auth Hardening (closed)"), USER_GUIDE §6 (строка `AUTO_BI_AUTH_COOKIE_SECURE`)/§7 (абзац про rate-limit/cookie/хэш). **`.env.example` НЕ трогала** — main сейчас без auth-блока вообще (полный `.env.example` — зона S03, который висит незамерженным на `docs/s03-doc-sync`/`c8e52c2`, уже содержит AUTH-блок); добавление `AUTO_BI_AUTH_COOKIE_SECURE` туда — при мерже S03 (или отдельной строкой сразу после). Работа шла в общем working tree параллельно с S04-сессией (гигиена/dependabot) — коммитила/буду коммитить точечно (не `-A`), не трогала файлы, которые правила она (`.env.example`/README.md/CLAUDE.md/docs/history/*). Закрывает B-2, B-3, B-4.

- [x] ~~**S07 — Ops-hardening**~~ · — · Sonnet · M · зависит: — · **ГОТОВО (2026-07-04)**
  ~~`/api/v1/ready` (store + DWH `SELECT 1` + BI healthcheck + опц. LLM); `--log-level` / структурный лог; фикс B-7 (failed-запись оборванного билда). Закрывает B-6, B-7, O-3.~~ — СДЕЛАНО: **B-6** новый `GET /api/v1/ready` (открыт даже при `auth_enabled`, как `/health`) — `store.ping()` + `run_query("SELECT 1")` + `adapter.healthcheck()` на Superset (v1 BI-таргет) гейтят `{"ok": false}`/503; LLM-проверка (GraceKelly — живой `GET {url}/health`; Anthropic — без вызова, чтобы не жечь токены на каждый пинг) репортится, но **не** гейтит `ok` — ровно как задумано «опц.». Невырезанная зависимость (store/run_query/bi_healthcheck не передан в `create_app`) не гейтит, репортится `configured: false`. **B-7** `agent/pipeline.py::compile_and_build` — весь конвейер (label-joins→top-N→validate→SQL-guard→healthcheck→build) теперь под ОДНИМ `try/except` вместо узкого except вокруг только `adapter.build()`: сессия помечается `building` в начале, любое исключение (включая `SpecValidationError`/healthcheck-`RuntimeError`, которые раньше пропадали без следа) пишет `failed`-билд+статус. Новый `Store.reap_stuck_builds()` — вызывается при старте `auto_bi serve`, находит сессии, застрявшие в `building` с прошлого (убитого/упавшего) процесса, дописывает синтетическую `failed`-строку в `builds` + переводит сессию в `failed` — рестарт больше не теряет факт оборванного билда. **O-3** новый `auto_bi/logging_setup.py::configure_logging` — `serve --log-level {DEBUG..CRITICAL} --log-format {text,json}`; `json` отключает dictConfig uvicorn (`log_config=None`), так что `uvicorn`/`uvicorn.access`-логгеры тоже льются в настроенный root-логгер одним потоком. Операционные `print()` в `_serve` (auth-seed, reap-отчёт) переведены на `logger.info`. Гейт: ruff/black/mypy 0/68 · pytest **623**/1 skipped(нет `anthropic`-extra, ожидаемо)/32 deselected, 95% coverage (+20: 8 `test_api_ready.py` + 5 `test_logging_setup.py` + 5 `test_store.py` (ping×2/session_status getter/reap×2) + 2 `test_pipeline.py` (SQL-guard и healthcheck теперь пишут `failed`)) · advisor-eval 9/9. Доки: ARCHITECTURE §3.11 (новая подсекция Ops-hardening), USER_GUIDE §4 (`--log-level`/`--log-format`, `/api/v1/ready`). Закрывает B-6, B-7, O-3.

- [x] ~~**S08 — Deployment-гайд** (`docs/DEPLOYMENT.md`) · — · Sonnet · S · зависит: S06, S07~~ · **ГОТОВО (2026-07-04)**
  ~~workers=1 (обоснование: in-memory sessions/SSE), reverse-proxy+TLS, бэкап SQLite, ротация jsonl, compose-пример прод-запуска, чеклист секретов. Закрывает P-4 (документационно), O-1.~~ — СДЕЛАНО: новый `docs/DEPLOYMENT.md` (9 разделов) — §1 обоснование `workers=1` (in-memory `SessionManager`/`ManagedSession` + буферизованный SSE в `api/sessions.py`, process-local по конструкции); §2 запуск (Docker/`uv run`, обязательные volume'ы `data/`+`logs/`); §3 reverse-proxy+TLS (nginx/Caddy примеры, SSE-специфичные `proxy_buffering off`, и явная гоча — авто-эвристика `AUTO_BI_AUTH_COOKIE_SECURE` смотрит на `--host`, не на внешний TLS, за proxy'ем выставлять явно); §4 `/api/v1/ready` для оркестратора (уже полностью собран в `auto_bi serve`); §5 `docker-compose.prod.yml`-пример (app+Caddy) + systemd-юнит; §6 бэкап SQLite через `sqlite3 .backup` (безопасен без WAL, в отличие от голого `cp`) + cron + litestream как опция; §7 ротация `logs/llm_calls.jsonl` через `logrotate --copytruncate` (файл держится открытым на дозапись); §8 чеклист секретов; §9 что переживает рестарт (`reap_stuck_builds`, S07) и что нет (in-memory диалоги → `X-4`). README.md: строка в таблице документации. Docs-only, гейта нет (ruff/black/mypy/pytest не затронуты). Закрывает P-4 (документационно), O-1.

- [x] ~~**S09 — CI-integration-стенд**~~ · — · Sonnet · M · зависит: — · **ГОТОВО (2026-07-04)**
  ~~Job в Actions: docker compose (CH + Superset, `DEMO_FACT_ROWS=1e6`) → `pytest -m integration` → живой `auto_bi build --auto` в Superset; retry на warm-up Superset. Закрывает T-1.~~ — СДЕЛАНО: новый job `integration` в `.github/workflows/ci.yml` (параллельно `quality`) поднимает тот же `docker-compose.yml`, что и Mac-стенд, с `DEMO_FACT_ROWS=1000000` (вместо дефолтных 100M) — `docker compose up -d --build --wait --wait-timeout 900` использует уже существующие healthchecks сервисов как retry-логику на Superset warm-up (не заводил отдельный polling-скрипт). Живьём гоняются `pytest -m integration tests/test_superset_contract.py` (21 тест, все viz_type + native-filters roundtrip) и `auto_bi build --auto dm.sales_daily --target superset` (auto-overview без LLM — секреты/ключи не нужны вообще). DataLens вне скоупа (self-hosted стенд, в `docker-compose.yml` не описан, остаётся Mac-only). Доки: ARCHITECTURE §3.12 (новая), README-абзац про интеграционные сьюты обновлён (больше не «в CI не запускаются» блоком). **Верификация:** т.к. Docker не запускается локально на этой машине (Windows), гонял через PR #6 → живой прогон на GH Actions ubuntu-latest — `integration`-job зелёный за 1m39s, `21 passed` в contract-сьюте + реальный `Дашборд готов: .../superset/dashboard/2/` (8 чартов) в логе билда. PR смержен ff в main (`e3b2058`), push-триггер CI на main тоже зелёный (run 28693808393). Закрывает T-1.

- [x] ~~**S10 — Docker + release-конвейер**~~ · — · Sonnet · M · зависит: S09 (общий ci.yml — делать одной рукой) · **ГОТОВО (2026-07-04), тег/GHCR-публикация ⏸ ждут «да» владельца**
  ~~`docker build` на каждый PR; публикация GHCR по тегу; semver: bump 0.2.0, CHANGELOG.md, git tag + GH Release, `auto_bi --version`, версия в `/health`; coverage-бейдж из CI. Закрывает D-1, D-2, T-3.~~ — СДЕЛАНО: новый job `docker` в `ci.yml` (параллельно `quality`/`integration`) собирает образ на каждый push/PR — только сборка, без push/login, ловит дрейф `Dockerfile` (D-2, который ни разу не собирался). Новый **`release.yml`** — отдельный workflow, триггер только на push тега `vX.Y.Z`: собирает образ через `docker/build-push-action`, пушит в GHCR (`ghcr.io/<repo, lowercase>:<version>`+`:latest`) через `GITHUB_TOKEN` (без доп. секретов), вырезает из `CHANGELOG.md` секцию `## [<version>]` (`awk`) и создаёт GitHub Release с этим текстом (`softprops/action-gh-release`). **`auto_bi --version`** (argparse `action="version"`) + **поле `version` в `GET /api/v1/health`** — оба читают `auto_bi.__version__` (единственный источник, вручную бампится, CI не автобампит). Версия **0.1.0→0.2.0** (`pyproject.toml`+`auto_bi/__init__.py`). Новый **`CHANGELOG.md`** (Keep a Changelog) — секция `[0.2.0]` документирует накопленный функционал Phase 0–4+hardening (это первый версионированный релиз проекта, до этого `git tag`/Releases были пусты — D-1). **Coverage-бейдж генерируется CI, не правится руками (T-3, было статичное число, дрейфовавшее — D-3):** шаг `Coverage badge data` в `quality`-job читает `.coverage` (`coverage report --format=total`), пишет `.github/badges/coverage.json` в shields.io endpoint-схеме; README ссылается через `img.shields.io/endpoint`. Коммит бейджа обратно в main — отдельный шаг, гейтится `push`+`main` (никогда на PR) и пропускается при отсутствии изменений (`git diff --quiet`, коммит `[skip ci]`); `quality`-job получил `permissions: contents: write` только под этот шаг. Гейт: ruff/black/mypy 0/68 · pytest **624**/1 skipped(нет `anthropic`-extra, ожидаемо)/32 deselected, 95% cov (+1: `--version`-тест) · advisor-eval 9/9. Доки: ARCHITECTURE §3.13 (новая), README (динамический бейдж, ссылка на CHANGELOG, абзац про docker/release job'ы). **Верификация без локального Docker** (Windows-машина Docker не гоняет): запушила ветку в **PR #7**, дождалась живого прогона на GH Actions ubuntu-latest — все 3 job'а (`docker` 18с, `integration` 1м26с, `quality` 34с) зелёные; ff-merge в main (`452a8d5`), push-триггер CI на main тоже зелёный (run 28695238500), шаг коммита бейджа корректно отработал no-op («Badge unchanged, nothing to commit» — значение не изменилось 95%→95%, коммит не создан, поведение по дизайну). **⚠️ НЕ сделано намеренно: реальный тег `v0.2.0` не вырезан, `release.yml` ни разу не выполнялся живьём** — цикл «git tag + GH Release + первая публикация образа в GHCR» = первый публичный релиз проекта (первый контейнер-образ под аккаунтом владельца, первая публичная GitHub Release) — вынесла на явное подтверждение владельца перед первым запуском, а не вырезала тег самостоятельно, несмотря на то что S10 в этом трекере без гейта (см. диалог сессии). Закрывает D-1, D-2, T-3.

- [~] **S11 — Golden-eval в CI** · — · Fable (дизайн replay-контракта) → Sonnet (импл.) · M · зависит: S01 · **МЕХАНИЗМ ГОТОВ (2026-07-04), фикстуры+CI-шаг НЕ сделаны**
  Record/replay-фикстуры LLM-ответов (вкл. новые кейсы S01) → offline-golden на каждый PR; weekly-job с живым ключом — опционально. Закрывает T-2.
  ~~Владелец выбрал «только design-контракт» (S01 не смёржен — квота Perplexity на паузе, реальных S01-кейсов на main нет).~~ — СДЕЛАНО (контракт+механизм, на 26 кейсах `main`): `auto_bi/llm/fixture.py` — `FixtureLLMClient`(replay)/`RecordingLLMClient`(record), оба структурно = `LLMClient` (подключаются на том же шве, что GraceKelly/Anthropic-клиенты, без нового интерфейса). Фикстура = один JSON на кейс (`<fixtures_dir>/<case_id>.json`, `calls:[{step,schema,response}]`), повторяющий ровно последовательность `complete()`-вызовов кейса. `eval/runner.py::run_golden_case` дак-тайпингом дёргает `begin_case(case_id)`/`end_case()` на `llm`, если они есть — так один shared LLM-клиент знает, чью фикстуру читать/писать; расхождение (промпт/схема сменились) = громкая `FixtureMissingError`, не тихий чужой ответ. Новый CLI: `auto_bi eval --suite golden --llm-mode {live,replay,record} --fixtures-dir DIR` (дефолт `live`, старое поведение не меняется). Смоук-тест CLI: рукописная фикстура для `g1_revenue_by_day` (того же качества, что `GOOD_SPEC`/`CLEAR_REPORT` в тестах) → `--llm-mode replay` PASS офлайн, без GraceKelly/Anthropic. Гейт: ruff/black/mypy 0/69 (новый модуль) · pytest **635**/1 skipped/32 deselected, 95% cov (+11: `test_llm_fixture.py` + 2 hook-теста в `test_eval.py`) · advisor не перезапускался (без изменений). Доки: ARCHITECTURE §3.14 (новая). **НЕ сделано намеренно — отдельный шаг, живой прогон = квота/деньги:** реальные фикстуры для 26 кейсов `main` не записаны (GraceKelly сожгла бы Perplexity-квоту, которая и так на паузе ради S01-хвоста; Anthropic не настроен — нет ключа, нет `anthropic`-extra в этом окружении) → CI `quality`-job НЕ получил шаг `--llm-mode replay` (нечем было бы его наполнить). Т-2 закроется полностью, когда появятся реальные фикстуры: **следующий шаг** — владелец выбирает чем записывать (GraceKelly-квота или Anthropic-ключ+`uv sync --extra anthropic`), затем `auto_bi eval --suite golden --llm-mode record --fixtures-dir tests/fixtures/golden_llm`, проверить `--llm-mode replay` тем же набором, закоммитить `tests/fixtures/golden_llm/*.json`, добавить шаг в `ci.yml` рядом с advisor-эвалом. Когда S01 смёржен — те же record/replay кейсы делаются и для его новых `expect_transforms/ratio/grain/bins/lag`-кейсов.

- [x] ~~**S12 — DataLens-стенд-сессия №1: C7 + C2**~~ · 🔵 стенд · **Opus** · M · зависит: живой Mac-стенд · **ГОТОВО (2026-07-04, live-verified на стенде deproject-mac)**
  ~~C7 histogram sort (кандидат: `sort=[item(bucket)]` ASC — проверить числовой vs лексикографический sort discrete-поля на живом стенде) + C2 категориальная ось; render-verify скринами ДО «готово»; НЕ реверсить вслепую (autobi-bi-engine-limits). Закрывает B-1.~~ — СДЕЛАНО. **Ключевой вывод: C7 — НЕ баг.** Гипотеза аудита («корзины гистограммы рендерятся лексикографически, „50" после „350"») была code-inspection-догадкой, НЕ live-verified — и на живом стенде НЕ подтвердилась. Факт (3 live-теста через `/api/run` + визуальный скрин дашборда `s12_histogram_numeric_order_verified.png`): DataLens сортирует numeric-string-cast категориальную ось **ЧИСЛЕННО** по умолчанию → корзины рендерятся 50,100,…,350 без всякого `sort`-поля, ДАЖЕ когда subselect вернул их перемешанными (скрамбл-тест: SQL `50,350,300,250,100,200,150` → рендер `50,100,…,350`). Догадка «категориальная ось→alphabetical» верна лишь для НАСТОЯЩИХ строк (имена городов → measure-sort у horizontal bar), не для числовых корзин. **Фикс НЕ нужен и был бы вреден** (numeric sort без явного direction флипает бары в DESC — проверено вариантом v1). **C2 (форс дискретной оси числового измерения) — подтверждён live** (string-cast B2 уже в коде; на стенде рендерит широкие дискретные бары, не непрерывную «стену»). Изменения кода: dated live-verify комментарий в `chart_config.py` (чтобы будущая сессия не «починила» это обратно в баг) + integration-guard `test_histogram_buckets_render_in_numeric_order` в `test_datalens_contract.py` (защита от дрейфа плавающего тега образа стенда). Гейт: ruff/black/mypy 0 · unit `test_datalens_chart` 36 passed · **live integration `test_datalens_contract` 12 passed** (все viz-типы + histogram numeric order + dashboard idempotency, стенд deproject-mac). Закрывает B-1 (как не-баг) и C2. **След. стенд-задача → S13 (B5 + C1), стенд сейчас жив.**

- [ ] **S13 — DataLens-стенд-сессия №2: B5 + C1 (time-boxed)** · 🔵 стенд · Opus · M · зависит: S12 (тот же стенд)
  B5 преднастроенный период (defaultDataMask в обоих BI); C1 percent-ось — 0.5 дня, при неудаче честно закрыть как engine-limit в docs и памяти.

- [x] ~~**S14 — yoy-KPI скаляр (B1 роадмапа)**~~ · 🟠 S4 · design (Opus) → impl (Opus) · M · зависит: «go» владельца · **ГОТОВО (2026-07-04), СМЁРЖЕНО (main=`2823e92`, PR #8)**
  ~~Design-doc «предложи варианты» (скалярный period-compare примитив) → go → имплементация + CH live-verify + autospec-интеграция (KPI «+X% г/г» при ≥2 годах). Закрывает P-5.~~ — СДЕЛАНО. Design-doc `docs/plans/2026-07-04-s14-yoy-kpi-scalar.md` (3 варианта); **владелец выбрал Вариант A** (скаляр-% как отдельная плитка через условную агрегацию) **+ «последний присутствующий бакет»** для v1. Новый `Measure.compare: ScalarCompare {column, grain, kind: yoy|pop, output: pct|abs}` — KPI, значение = одно число (последний период vs год/период назад); в отличие от `yoy_pct` (оконный РЯД) сворачивается до ОДНОЙ строки через условную агрегацию по двум бакетам (`_generate_compare_kpi_sql`, отдельный путь SQL_GEN) → big_number остаётся истинным скаляром (нет окна/измерения); отсутствующий бакет год назад → NULL. Контракт на `Measure` (не query) → **нулевые правки адаптеров** (процентный big_number уже рендерится: Superset d3 `.1%` / DataLens percent). Валидация: только big_number, non-day grain, TIME-колонка, взаимоисключимо с transform/denominator. Autospec (≥2 года) добавляет плитку «`<герой>`, г/г» рядом с уровнем, **заместив полноширинную yoy-линию** (тот же инсайт компактнее → structure/share выживает в 8-чартовом бюджете). **Верификация 3×:** офлайн (ruff/black · mypy 0/69 · pytest **655** +31 · advisor 9/9 · cov 95%) + DuckDB PG-числа + **CH live-verified на стенде** (`verify_live_clickhouse.py`: yoy-скаляр = ручной расчёт) + **CI integration зелёный** (живой CH+Superset build). ARCHITECTURE §3.4 обновлён. Закрывает P-5.

- [x] ~~**S15 — Публичный деплой демо (E1)** · 🔴 наружу + 🟣 · Opus (инфра) + владелец (ключ/хостинг) · L · зависит: S02, S06, S07, S08, S10~~ · **ГОТОВО (2026-07-07, Fable; вариант владельца: HF Space, auto-only БЕЗ LLM/ключа)**
  ~~VPS/Fly/Render: контейнер + лёгкий CH-синтетика + Superset + Anthropic-ключ + квоты (per-IP лимит сессий/день, O-2) + auth/read-only режим; мониторинг расходов ключа. Закрывает P-3, O-2.~~ — СДЕЛАНО как **P8**: живое демо `https://juliome20-auto-bi-demo.hf.space` (HF Space `JuLioMe20/auto-bi-demo`, RUNNING cpu-basic; НОВЫЙ HF-акк владельца — на liovina квоты не было). Один контейнер CH 24.8 + Superset 4.1.2 + auto_bi (uv-managed py3.12) + nginx (`/agent/`→auto_bi со срезом префикса, корень→Superset); режим `AUTO_BI_DEMO_AUTO_ONLY=true` — LLM не подключён вовсе (решение владельца: ноль расходов, text-путь показан GIF'ом в README), text/fields/enrichment → 403, вкладки UI задизейблены. Аноним смотрит дашборды через read-only Public-роль (`PUBLIC_ROLE_LIKE=Gamma` МИНУС write-пермишны — иначе FAB is_item_public пускает POST /chart анонимом мимо JWT, ключевая гоча) + `all_datasource_access`; `SUPERSET_SECRET_KEY` один на контейнер (per-import ключ рвал JWT между воркерами). Предпосылки F-2 (прокси-wiring квот) + L-4 (purge ключей лимитера) закрыты кодом+тестами. Верификация: workflow `demo-image.yml` (build+smoke полного пути, зелёный) + live на Space (curl: 403-гейт/билд/аноним-200 + Playwright-скрины UI и дашборда с данными «12 млрд ₽»). Упаковка: `deploy/hf-demo/` (README с процедурой публикации). Закрывает P-3; O-2 был закрыт кодом ранее (в auto-only демо квота не нужна — LLM-эндпоинты 403).

- [ ] **S16 — Демо-GIF/видео в README** · 🟣 (asset-приёмка) · Opus (Playwright-запись) + владелец · S · зависит: S01
  Полный цикл: текст → уточнение → spec+advisor → build → дашборд. Можно на локальном стенде до/вместо S15.

- [ ] **S17 — Финальный адверсариальный аудит + re-score** · — · **Fable** · M · зависит: минимум S01–S12 (S15 желателен, но 9.8 достижим с S16 вместо живого демо)
  Свежий состязательный проход по коду (паттерн `6d0c8da`); замер latency пайплайна на живом стенде; сверка DoD; вердикт 9.8.

- [~] **S18 — Mistral-СТРАХОВКА** · 🟣 владелец · Sonnet · S · **⏸ ОТЛОЖЕН (2026-07-04): Mistral проверен живьём → слаб на ядре → владелец «не используем»**
  Провайдер РЕАЛИЗОВАН и живо-проверен, но НЕ смёржен (ветка `feat/mistral-provider`=`96d0af0`, поверх S01; гейт зелёный mypy 0/68 · pytest 620 · advisor 9/9; запаркована — забракованное не пушу): `MistralClient` (OpenAI-совместимый REST `/v1/chat/completions` + retry-backoff на 429 free-tier + usage-токены), provider-aware eval-баннер (`cli.py`), 10 юнит-тестов (MockTransport). Живой прогон S01-golden `mistral-large-latest`: **12/14** (ядро+ambiguous+infeasible ✓; ✗ mom g18 + lag-N g19 → невалидный spec, слабость модели). **Решение владельца: раз Mistral слаб на ядре — как fallback не используем.** Ключ Mistral (32 симв) записан в worktree `.env` (`AUTO_BI_MISTRAL_API_KEY`, gitignored). Возобновить — только если появится сильная резервная модель; текущий Mistral-large для fallback недостаточен. Исходный замысел (fallback GK→Mistral при transport-fail/breaker) — в git-истории коммита `96d0af0`.

### Опциональные усилители (НЕ требуются для 9.8)

- [ ] **X-1 — Luxms-адаптер (C4)** · 🔵 креды sandbox / Docker-стенд · Opus · L — план готов: `docs/plans/2026-06-14-luxms-adapter-plan.md`
- [x] ~~**X-2 — PyPI-публикация** · 🟣 · Sonnet · S~~ · **ОПУБЛИКОВАНО (2026-07-10, Fable): `pip install autobi-agent` (v0.3.1), verified установкой из индекса.** Trusted publisher заведён, release-workflow оба job'а (`release`+`pypi`) SUCCESS. Имя `auto-bi` PyPI отверг (too-similar к заглушке `autobi` — сравнение схлопывает `-`/`_`/`.`); дистрибутив переименован в `autobi-agent`, импорт-пакет и CLI остались `auto_bi`. Гейты: ruff/pytest 750/2 skipped, twine PASS, wheel-смоук из чистого venv.
- [x] ~~**X-3 — A5 когорты** · 🟠 S4 + демо-модель с entity · Fable→Opus — по явному запросу~~ · **ГОТОВО (2026-07-11, Fable; дизайн «синонимы + паттерн» утверждён владельцем)** — «покажи retention» словами, без имени витрины: (1) `synonyms` у Table/Column семантической модели (рендер в промпты `[синонимы: …]` без гейта include_samples + скоринг контекст-селекции с весом имени 3.0/2.0) — `model_stand.yaml` размечен для обеих когортных витрин; (2) когортный паттерн SPEC_RULES п.12 (по ФОРМЕ витрины: время × периодов-с-начала × число субъектов → heatmap-треугольник + bar период=1 + KPI период=0, мера абсолютная, denominator запрещён). IR/BIAdapter НЕ тронуты (S4 не понадобился — витрина уже была). Гейт: pytest 759/2 skipped · cov 95% · advisor 9/9 · replay 37/37+16/16 · **live e2e на стенде: `build "Покажи удержание клиентов" --target datalens` → grounding нашёл витрину ТОЛЬКО по синонимам → spec = каноническая тройка → дашборд `/ocbl3u4qkjeu7`, 3/3 чартов с данными**. Тесты: `tests/test_x3_retention.py`; ARCHITECTURE §3.2/§3.3.
- [x] ~~**X-4 — Session-resume после рестарта** (гидрация из Store) · — · Opus — радикально закрывает P-4; для single-user достаточно S08~~ · **ГОТОВО (2026-07-07, Fable)** — ленивая регидрация в `SessionManager.get()` (schema v7: `sessions.owner/target_bi/pinned`; фаза из spec-строк, clarify-раунды из trace, билд+абсолютный url из builds, RBAC-скоуп владельца восстановлен, DELETE=tombstone, синтетический терминальный SSE-event). Гейт: mypy 0/69 · pytest 738 (+16) · advisor 9/9 · replay 37/37+16/16 · живой smoke: реальный kill/restart `auto_bi serve` → сессия воскресла, DELETE после рестарта → 404. Дизайн/тесты: `x4-session-resume.md`, ARCHITECTURE §3.15.
- [x] ~~**X-5 — Escape hatch `raw_sql`**~~ · **ГОТОВО (2026-07-11, Opus; вариант «ручной люк» утверждён владельцем)** — `ChartQuery.raw_sql` (только `viz=table`) SQL_GEN отдаёт ДОСЛОВНО → тот же live-гейт, что у сгенерированного SQL (`guard_sql` SELECT-only → EXPLAIN → LIMIT-прогон в `compile_and_build`) → virtual dataset в Superset. **Ручной люк** (CLI `auto_bi raw --sql-file q.sql`), НЕ LLM-генерация — инвариант 1 сохранён (NL→SQL в ядро не брали: `nl_sql_decision_03_07_26.md` §3). Ров ослаблен осознанно: advisor слепнет, нормализации B1/B3 и форматы off, колонки по модели не проверяются, `table` — лишь метка датасета. Границы: только Superset (v1), нельзя мешать с агрегирующими IR-полями. Гейт: pytest +13 (`tests/test_x5_raw_sql.py`) · ruff/black/mypy clean. Доки: ARCHITECTURE §3.16, CHANGELOG.

---

## Граф зависимостей

```mermaid
graph LR
    subgraph Волна 1 — параллельно, без внешних гейтов
        S01[S01 F1 text-first ядро · Fable]
        S02[S02 Anthropic first-class · Sonnet]
        S04[S04 repo-гигиена · Haiku]
        S05[S05 community-файлы · Haiku]
        S06[S06 security-hardening · Sonnet]
        S07[S07 ops-hardening · Sonnet]
        S09[S09 CI integration · Sonnet]
    end
    subgraph Волна 2
        S03[S03 doc-sync · Sonnet]
        S08[S08 deployment-гайд · Sonnet]
        S10[S10 docker+release · Sonnet]
        S11[S11 golden-eval в CI · Fable→Sonnet]
        S12[S12 стенд DataLens-1 C7/C2 · Opus]
        S14[S14 yoy-KPI S4 · Fable→Sonnet]
    end
    subgraph Волна 3 — наружу
        S13[S13 стенд DataLens-2 B5/C1 · Opus]
        S15[S15 деплой демо · Opus+владелец]
        S16[S16 GIF/видео · Opus+владелец]
        S17[S17 финальный аудит · Fable]
    end
    S02 --> S03
    S06 --> S08
    S07 --> S08
    S09 --> S10
    S10 --> S03
    S01 --> S11
    S01 --> S16
    S12 --> S13
    S02 --> S15
    S06 --> S15
    S07 --> S15
    S08 --> S15
    S10 --> S15
    S01 --> S17
    S03 --> S17
    S10 --> S17
    S11 --> S17
    S12 --> S17
    S15 -.опц..-> S17
    S14 -.owner go.-> S17
```

Текстом (кто кого ждёт):

- **Ни от кого не зависят (стартовать параллельно):** S01, S02, S04, S05, S06, S07, S09.
- S03 ← S02 (+S10 для бейджа) · S08 ← S06+S07 · S10 ← S09 · S11 ← S01 · S13 ← S12 · S16 ← S01.
- S12/S13 ждут только живой Mac-стенд; S14 — «go» владельца; S15 ← S02+S06+S07+S08+S10 + решение владельца.
- S17 — последним, после минимум S01–S12.

**Критический путь:** `S01 → S11 → S17` и `S09 → S10 → S15 → S17`. Самые длинные куски — S01 (одна плотная Fable-сессия) и S15 (owner-gated).

---

## Порядок сессий (рекомендация)

1. **Сессия A (Fable):** S01 — главный анлок, ничего его не блокирует.
2. **Сессия B (Sonnet, параллельно):** S06 → S07 → S02 (после слова владельца) → S03.
3. **Сессия C (Sonnet):** S09 → S10 — оба трогают ci.yml, делать одним заходом.
4. **Сессия D (Haiku):** S04 + S05 (вопрос про cont.8 — владельцу заранее).
5. **Сессия E (Opus, когда жив стенд):** S12 → S13.
6. **Сессия F (Fable):** S11 (+ S14-design, если владелец дал «go»).
7. **Сессия G (Opus + владелец):** S15 → S16.
8. **Сессия H (Fable):** S17 — финальный аудит и вердикт.

## Выбор моделей — почему так

- **Fable** — где решается «что правильно»: S2-промпты + eval-дизайн (S01, S11-контракт), S4-контракт (S14-design), финальный адверсариальный аудит (S17). Дисциплина проекта: промпт-правки = Fable/ручная, НЕ auto.
- **Opus** — длинные автономные сессии с внешним миром: стенд-реверс DataLens с render-verify (S12/S13), инфра-деплой (S15), Playwright-запись (S16).
- **Sonnet** — стандартная имплементация с чётким ТЗ: hardening (S06/S07), CI (S09/S10), доки (S03/S08), интеграция замыслов Fable (S11, S14-impl).
- **Haiku** — чистая механика без решений: файлы, ветки, шаблоны (S04, S05).

## Вопросы владельцу (собрать одним батчем)

1. Смена LLM-дефолта на anthropic (S02) — ок?
2. `wip/cont8-dashboard-heatmap` — выбросить / оставить / доделать? (висит с 2026-06-26)
3. yoy-KPI (S14): открывать S4-трек?
4. Демо (S15): хостинг/бюджет/ключ — когда и где? Если «пока нет» — S16 (видео) вместо, 9.8 достижим и так.

## Анти-скоуп (не делать)

Не переписывать web UI · не добавлять $-учёт (осознанный non-goal) · не плодить advisor-правила без реальных кейсов · не тащить LangChain/оркестраторы · не строить multi-tenant SaaS. 9.8 достигается упаковкой того, что есть.
